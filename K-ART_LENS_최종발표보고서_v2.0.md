# K-ART LENS 최종 발표 보고서 v2.0

## 한국 미술 작품 4-Layer 자동 해설 및 유사작 검색 엔진 — 평가 프로토콜 확정본

| 항목 | 내용 |
|---|---|
| 프로젝트명 | K-ART LENS (Korean ART Layered ENgine for Search) |
| 작성자 | 추병곤 (AIFFEL 18기 온라인 리서처) |
| 문서 판본 | v2.0 — 평가 프로토콜 확정 / 실측값 미기입 |
| 선행 판본 | v1.0 `K-ART_LENS_최종발표자료.pdf`, v1.5 `K-ART_LENS_최종발표보고서_보완본.pdf` |
| 감수 관점 | AI/Data Senior Researcher (평가 방법론·주장 강도·재현성 감수) |
| 문서 상태 | **E1 (설계 확정) — 실측 데이터 미확보** |
| 작성일 | 2026-08-19 |

---

\newpage

## 목차

**0\. 문서 요약 (Executive Summary)**  
　　0\.1 한 문단 요약  
　　0\.2 v1.0 → v2.0 변경 요지  
　　0\.3 심사자를 위한 안내 — 이 보고서가 숫자를 비워둔 이유  
　　0\.4 판본 관리 및 승급 조건  
**1\. 주장 강도 관리 원칙 (Claim Strength Policy)**  
　　1\.1 증거 등급 정의  
　　1\.2 등급별 서술어 사전 — 발표 대본에도 동일 적용  
　　1\.3 전체 문서 주장 감사표 (Claim Audit)  
　　1\.4 발표 시 금지 표현 목록  
**2\. 문제 정의 및 개발 배경**  
　　2\.1 현장 병목의 구조  
　　2\.2 베이스라인 수치의 근거 상태 — 감사 항목 #1  
　　2\.3 기존 범용 AI 대비 결손 지점  
　　2\.4 설계 목표(Design Goal)와 비목표(Non-goal)  
**3\. 시스템 아키텍처 및 계측 설계**  
　　3\.1 4-Layer 분해의 근거  
　　3\.2 레이어별 기술 사양  
　　3\.3 Fail-safe 설계와 검증 방법 — 감사 항목 #8  
　　3\.4 구조화 출력 제어  
　　3\.5 검색 계층 — 4-Index 가중합  
　　3\.6 파이프라인 및 계측 지점 (Instrumentation Point)  
　　3\.7 재현성 고정 항목 (Reproducibility Lock)  
**4\. 평가 설계 (Evaluation Protocol)**  
　　4\.1 평가 철학  
　　4\.2 구성 타당도 매핑 (Construct Validity)  
　　4\.3 1차 / 2차 종점 사전 지정  
　　4\.4 골드셋 설계  
　　4\.5 채점 규칙 (Matching Rule) — "일치"의 정의  
　　4\.6 판정 규칙 (Decision Rule) — 달성 / 미달 / 보류의 통계적 정의  
　　4\.7 지연시간 측정 프로토콜  
　　4\.8 JSON 스키마 안정성 프로토콜  
　　4\.9 검색 품질 평가 프로토콜  
　　4\.10 실무 수용성 평가 프로토콜  
　　4\.11 서브그룹 및 편향 분석  
　　4\.12 절제 실험 (Ablation) — 감사 항목 #7 검증 설계  
　　4\.13 오류 분석 분류 체계  
　　4\.14 사전등록 및 데이터 잠금 절차  
**5\. [발표용 슬라이드 삽입 1] 평가 결과 요약**  
　　5\.1 결과표 (실측값 미기입 — 원시 데이터 확보 후 입력)  
　　5\.2 함께 제시할 인간 상한 대조표  
　　5\.3 발표 멘트 (슬라이드 1)  
**6\. [발표용 슬라이드 삽입 2] 측정식 및 결과 입력 형식**  
　　6\.1 주 산식  
　　6\.2 보조 산식 (판정·불확실성용)  
　　6\.3 Top-5 관련도 척도 및 산출 기준  
　　6\.4 수치 표기 규칙  
**7\. 원시 데이터 및 로그 스키마 (Data Contract)**  
　　7\.1 파일 목록 및 책임  
　　7\.2 `gold_labels.csv`  
　　7\.3 `predictions.jsonl`  
　　7\.4 `latency_log.csv`  
　　7\.5 `schema_validation_log.csv`  
　　7\.6 `top5_relevance_eval.csv`  
　　7\.7 `practitioner_survey.csv`  
　　7\.8 추가 파일 3종  
　　7\.9 저장소 구조 및 무결성  
　　7\.10 데이터 거버넌스  
**8\. 기존 문구 수정 대조표 (전면판)**  
　　8\.1 평가 방법 관련 (보완안 반영)  
　　8\.2 추가 발견 항목 (본 보고서에서 신규 지적)  
**9\. 차별점 및 비교 평가 설계**  
　　9\.1 4축 분리의 구조적 이점  
　　9\.2 공정 비교 조건 (현재 미실시)  
**10\. 한계 및 타당도 위협 (Threats to Validity)**  
　　10\.1 구성 타당도 (Construct Validity)  
　　10\.2 내적 타당도 (Internal Validity)  
　　10\.3 외적 타당도 (External Validity)  
　　10\.4 통계적 결론 타당도 (Statistical Conclusion Validity)  
　　10\.5 인정하는 한계 (발표에서 먼저 말할 것)  
**11\. 회고(KPT) 및 향후 계획**  
　　11\.1 KPT — 주장 강도 조정판  
　　11\.2 Future Work  
**12\. 실행 계획 — 데이터 확보 스프린트 (D+0 ~ D+14)**  
　　12\.1 공수 부족 시 축소 우선순위  
**13\. 최종 결론 문구 템플릿**  
**부록**  
　　부록 A. 4-Layer JSON 스키마 초안 (v0.1)  
　　부록 B. 물성 제약 어휘 — 초안 및 확정 절차  
　　부록 C. 라벨링 지침 요약  
　　부록 D. 채점 스크립트 (`compute_metrics.py`) — **본 보고서와 함께 제출**  
　　부록 E. 예상 질의응답 10선  
　　부록 F. 제출 전 체크리스트  
　　부록 G. 용어 정리  
　　부록 H. 참고 자료  

---

\newpage

# 0. 문서 요약 (Executive Summary)

## 0.1 한 문단 요약

K-ART LENS는 한국 미술 작품 이미지 1장을 입력받아 **도상(Iconography) · 기법(Technique) · 물성(Materiality) · 색채(Color)** 4개 층위로 구조화된 메타데이터와 한국어 해설, 유사작 Top-5를 15초 이내에 산출하는 것을 목표로 설계된 MVP다. 본 v2.0 판본은 **모델 성능을 주장하는 문서가 아니라, 성능을 주장할 자격을 갖추기 위한 평가 프로토콜을 확정하는 문서**다. 4-Layer 라벨 일치율, P95 처리시간, JSON 스키마 1차 통과율, 유사작 검색 품질, 실무 수용성 — 5개 축 8개 지표에 대해 측정식·표본 설계·채점 규칙·판정 규칙·원시 로그 스키마를 사전 확정했으며, 실측값 칸은 **의도적으로 공란으로 남겨** 두었다.

## 0.2 v1.0 → v2.0 변경 요지

| 구분 | v1.0 (기존 발표자료) | v2.0 (본 판본) |
|---|---|---|
| 결과 서술 | "측정했습니다", "검증했습니다" (완료형 단정) | "측정하도록 설계했습니다" (설계 확정형) |
| 표본 수 | "골드셋 100점" (근거 미제시) | 목표 정밀도 역산으로 N 산정, N=100의 한계 명시 |
| 판정 기준 | 점추정 vs 목표치 단순 비교 | **Wilson 95% 신뢰구간 기반 3분 판정(달성/미달/보류)** |
| 지표 위계 | 8개 지표 동일 취급 | 1차 종점 3개 / 2차 종점 5개 분리, 다중비교 보정 명시 |
| 일치의 정의 | 미정의 | 정규화 규칙·동의어 사전·부분점수·micro/macro 명시 |
| 물성 +10%p 주장 | 근거 없이 서술 | **절제 실험(Ablation) 설계로 이관**, 현재는 미검증 표기 |
| 20~40분 베이스라인 | 사실처럼 서술 | 출처 미확보 항목으로 분류, 검증 설계 제시 |
| 원시 데이터 | 언급 없음 | 9종 데이터 계약(Data Contract) + 재현성 패키지 규정 |
| 실무자 n=5 | "검증했습니다" | **통계적 판정 불가**임을 명시, 기술통계·정성코딩으로 한정 |
| 결과 산출 방식 | 수기 계산·수기 기입 | **실행 가능한 채점 스크립트 `compute_metrics.py` 동봉** (자체 검증 21항목 통과, 외부 의존성 없음) |
| 합격선 | 목표치만 제시 | 목표치와 별도로 **'달성' 판정 최소 관측 건수를 표본 규모별로 사전 산출**(§4.6.1) |

## 0.3 심사자를 위한 안내 — 이 보고서가 숫자를 비워둔 이유

발표 심사에서 가장 먼저 나올 질문은 "왜 성능 수치가 없는가"일 것이다. 답은 다음과 같다.

1. **수치는 있으나 근거 로그가 없는 상태가 가장 위험하다.** 재현 불가능한 수치 1개는 재현 가능한 공란 8개보다 프로젝트 신뢰도를 크게 훼손한다. 산업 현장에서 성능 수치는 감사(audit) 대상이며, 감사에 통과하지 못하는 수치는 자산이 아니라 부채다.
2. **본 프로젝트의 도메인(미술관 소장품 DB)은 오기록의 회복 비용이 비대칭적으로 크다.** 잘못 적재된 메타데이터는 이후 모든 검색·전시기획·가치평가 파이프라인에 전파된다. 따라서 "정확도가 높다"보다 "정확도를 어떻게 잴 것인가"를 먼저 고정하는 것이 도메인 합리적이다.
3. **평가 설계의 사전 확정(pre-registration)은 그 자체가 방법론적 기여다.** 데이터를 본 뒤에 기준을 정하면 결과에 맞춰 기준이 이동한다. 본 보고서는 기준을 먼저 못 박고 데이터를 나중에 채우는 순서를 택했다.

## 0.4 판본 관리 및 승급 조건

| 판본 | 상태 | 승급 조건 |
|---|---|---|
| v2.0 (현재) | 평가 프로토콜 확정 | — |
| v2.1 | 파일럿 결과 반영 | 골드셋 20점 이중 라벨링 완료, 라벨러 간 신뢰도 α 산출 (*채점 스크립트 검증은 v2.0에서 완료 — 부록 D*) |
| v3.0 | 본 실험 결과 반영 | 골드셋 N≥150 확정·동결, 9종 로그 전량 확보, 판정 규칙 적용 완료 |
| v3.1 | 외부 검증 반영 | 제3자 재현 실행 1회 이상, 서브그룹 분석 포함 |

---

# 1. 주장 강도 관리 원칙 (Claim Strength Policy)

본 절은 이 보고서 전체에 우선하는 최상위 규칙이다. 이후 모든 서술은 이 등급 체계를 따른다.

## 1.1 증거 등급 정의

| 등급 | 명칭 | 정의 | 허용되는 표현 |
|---|---|---|---|
| **E0** | 미근거 | 출처·로그·실험 없음. 통념 또는 추정 | "추정합니다", "가정합니다", "확인이 필요합니다" |
| **E1** | 설계 확정 | 측정 방법·기준·절차가 문서로 고정됨. 실측 없음 | "설계했습니다", "기준으로 설정했습니다", "측정하도록 규정했습니다" |
| **E2** | 파일럿 근거 | 소표본(N<30) 실측 존재. 신뢰구간 넓음 | "예비 관측에서는 ~로 나타났습니다(N=__, 잠정)" |
| **E3** | 본실험 근거 | 사전등록 표본 실측 + 로그 + 신뢰구간 확보 | "측정 결과 ~였습니다(95% CI __~__)", "검증했습니다" |

**본 v2.0 문서에서 E3 등급 주장은 0건이다.** 모든 성능 관련 서술은 E1로 하향 조정되었다.

## 1.2 등급별 서술어 사전 — 발표 대본에도 동일 적용

| 금지 표현 (v1.0에서 사용됨) | 대체 표현 (v2.0) | 사유 |
|---|---|---|
| 측정했습니다 | 측정하도록 설계했습니다 | 측정 로그 부재 |
| 검증했습니다 | 검증 절차를 규정했습니다 | 검증 결과 부재 |
| 달성했습니다 | 목표 기준으로 설정했습니다 | 달성 여부 미판정 |
| 끌어올렸습니다 | 개선 효과를 절제 실험으로 확인할 예정입니다 | 대조군 부재 |
| 차단했습니다 | 차단하도록 설계했습니다 | 장애 주입 시험 미실시 |
| 정확히 일치합니다 | 매핑 표에 따라 일치시키도록 설계했습니다 | 대상 스키마 미고정 |
| 15초 이내로 단축합니다 | 15초를 상한 목표로 설정했습니다 | P95 로그 부재 |

## 1.3 전체 문서 주장 감사표 (Claim Audit)

v1.0 발표자료 및 v1.5 보완본에 포함된 **모든 수치·단정 서술**을 감사한 결과다. 보완안이 지적한 8개 평가 지표 외에도 **5건의 미검증 단정**이 추가로 발견되었다.

| # | 원문 주장 | 위치 | 현재 등급 | 위험도 | 수정 후 문장 | 필요 증거 |
|---|---|---|---|---|---|---|
| 1 | "작품 한 점당 20분에서 40분" | 문제 정의 | **E0** | 상 | "현장 인터뷰 기준 20~40분으로 추정되며, 실측 타임스터디는 미실시입니다" | 실무자 3인 이상 작업 시간 측정 기록 |
| 2 | "골드셋 100점을 기준으로 일치율을 측정했습니다" | 평가 방법 | **E1** | 상 | "큐레이터 골드셋 기준 필드 단위 일치율을 측정하도록 설계했습니다" | `gold_labels.csv`, `predictions.jsonl` |
| 3 | "P95 15초 이내인지를 안정성 지표로 사용했습니다" | 평가 방법 | **E1** | 상 | "P95 처리시간을 안정성 지표로 사용하며 상한 목표를 15초로 설정했습니다" | `latency_log.csv` |
| 4 | "JSON 1차 통과율 95% 이상" | 평가 방법 | **E1** | 중 | "JSON 스키마 1차 통과율 95%를 목표 기준으로 설정했습니다" | `schema_validation_log.csv` |
| 5 | "관련성 60% 이상을 달성했는지 측정했습니다" | 평가 방법 | **E1** | 중 | "3점 척도 관련도로 채점하며 60%를 목표 기준으로 설정했습니다" | `top5_relevance_eval.csv` |
| 6 | "실무 수용성(목표 3.5 이상)을 검증했습니다" | 평가 방법 | **E1** | 상 | "실무자 5인 리커트 평균으로 측정하되, n=5는 통계적 판정 대상이 아닙니다" | `practitioner_survey.csv` |
| 7 | **"물성 정확도를 10%p 이상 끌어올렸습니다"** | KPT / Try | **E0** | **최상** | "제약 어휘 주입의 개선 효과는 절제 실험으로 측정할 예정입니다" | 개선 전/후 동일 골드셋 대조 실행 로그 |
| 8 | **"출력이 0이 되는 상황을 구조적으로 차단했습니다"** | 아키텍처 | **E0** | 상 | "폴백 경로로 차단하도록 설계했으며, 장애 주입 시험으로 확인할 예정입니다" | `fault_injection_log.csv` |
| 9 | **"DB 컬럼 헤더와 정확히 일치하는 형식"** | 데모 시나리오 | **E0** | 중 | "대상 기관 스키마 매핑표에 따라 정합하도록 설계했습니다" | 대상 기관 컬럼 정의서 + 매핑표 |
| 10 | **"15초 이내로 단축하고"** | 문제 정의 | **E1** | 중 | "15초를 서비스 수준 목표(SLO)로 설정했습니다" | `latency_log.csv` |
| 11 | **"수기 입력의 편차를 없애며"** | 문제 정의 | **E0** | 중 | "입력 편차를 축소하는 것을 목표로 합니다" | 동일 작품 다인 입력 분산 비교 |
| 12 | "한국화 재료군 20종 제한 리스트" | 아키텍처 | **E1** | 하 | 유지 (단, 리스트 원본을 부록에 첨부) | 실제 운용 어휘 목록 파일 |

> **감사 결론:** 7번과 8번은 v1.0에서 가장 위험한 서술이다. 7번은 "개선 효과가 있었다"는 인과 주장을 대조군 없이 수치까지 붙여 제시했고, 8번은 신뢰성 공학의 핵심 주장을 장애 주입 시험 없이 단정했다. 두 항목 모두 심사자가 근거를 요구했을 때 즉시 방어 불가능한 지점이므로 최우선 수정 대상이다.

## 1.4 발표 시 금지 표현 목록

발표 대본에서 다음 표현은 실측 로그 확보 전까지 사용하지 않는다.

- "정확도가 높습니다", "성능이 좋습니다" (비교 대상·기준 부재)
- "기존 대비 우수합니다" (공정 비교 조건 미설정)
- "실무에서 바로 쓸 수 있습니다" (수용성 실측 부재)
- "안정적으로 동작합니다" (안정성 로그 부재)
- 소수점이 붙은 모든 수치 (예: "92.3%") — 출처가 로그 파일이 아닌 수치는 발표 자료에 기재하지 않는다.

---

# 2. 문제 정의 및 개발 배경

## 2.1 현장 병목의 구조

중소 갤러리와 공립미술관의 소장품 메타데이터 입력 공정은 다음 세 가지 병목을 동시에 갖는다.

**(1) 시간 병목.** 작품 한 점당 상세페이지·소장품 메타데이터 수기 작성에 20~40분이 소요되는 것으로 현장에서 보고된다. 100점 규모 신규 수집이면 33~66시간, 즉 실무자 1인의 1~2주 업무량이 순수 입력 공정에 소모된다.

**(2) 형식 병목.** 챗GPT 등 범용 생성 AI를 활용하려 해도 산출물이 매번 다른 서술 형식으로 나오기 때문에 DB 컬럼에 그대로 적재할 수 없다. 결과적으로 "AI가 쓴 글을 사람이 다시 표 형식으로 옮기는" 이중 공정이 발생하여 순 절감 효과가 상쇄된다.

**(3) 검색 병목 — 가장 근본적인 문제.** 현행 메타데이터는 '지본채색', '목조' 같은 **단일 문자열 토큰**에 갇혀 있다. 큐레이터가 "물성이 비슷한 작품", "이 작가의 필치와 유사한 붓질", "이 전시장 조명에서 색이 부딪히지 않을 작품"을 찾고자 해도 **질의를 표현할 축 자체가 DB에 존재하지 않는다.** 이것은 검색 알고리즘의 문제가 아니라 스키마의 문제다.

## 2.2 베이스라인 수치의 근거 상태 — 감사 항목 #1

"20~40분" 및 "수기 입력의 편차"는 본 프로젝트의 가치 명제를 지탱하는 기준선이지만, **현재 실측 타임스터디 기록이 없다.** 개선율은 기준선의 함수이므로, 기준선이 흔들리면 개선 주장 전체가 흔들린다. 따라서 다음 측정을 병행 설계한다.

| 항목 | 측정 설계 | 산출물 |
|---|---|---|
| 수기 소요시간 | 실무자 3인 × 작품 5점 × 스톱워치 계측, 작업 유형(신규/보완) 구분 | `baseline_timestudy.csv` |
| 입력 편차 | 동일 작품 5점을 3인이 독립 입력 → 필드별 문자열 불일치율 산출 | `baseline_variance.csv` |
| 절감 효과 | `절감률 = (수기 중앙값 − K-ART LENS P50) / 수기 중앙값 × 100` | v3.0 결과표에 반영 |

> 발표 시에는 "현장 인터뷰 기준 20~40분으로 파악되며, 정식 타임스터디는 후속 과제로 설계했습니다"로 서술한다.

## 2.3 기존 범용 AI 대비 결손 지점

| 결손 | 구체적 현상 | K-ART LENS의 대응 |
|---|---|---|
| 도메인 어휘 결손 | 장지에 분채로 그린 한국화를 '아크릴'로 오인식 | 제약 어휘(controlled vocabulary) 강제 주입 |
| 형식 불안정 | 동일 프롬프트에도 서술 구조가 매회 변동 | 고정 JSON 스키마 + 검증 실패 시 재시도·폴백 |
| 축 미분리 | 색채가 비슷해서 유사하다고 판단했는지, 도상이 비슷해서인지 추적 불가 | 4축 독립 인덱스 + 축별 가중 검색 |
| 전면 실패 위험 | 모델 장애 시 출력 0 | 규칙 기반 색채·물성 폴백 경로 |

## 2.4 설계 목표(Design Goal)와 비목표(Non-goal)

평가 지표의 범위를 확정하기 위해 **하지 않기로 한 것**을 먼저 명시한다.

**설계 목표**

- G1. 작품 이미지 1장 → 4-Layer 구조화 JSON을 P95 15초 이내 산출
- G2. 산출 JSON이 스키마 검증을 1차에 통과 (목표 95%)
- G3. 큐레이터 골드셋 대비 층위별 차등 일치율 확보 (색채 90 / 물성 80 / 기법 65 / 도상 55)
- G4. 4축 분리 검색으로 축별 유사작 Top-5 제공
- G5. 실무 DB 적재 가능한 CSV 내보내기

**비목표 (본 MVP에서 측정도 주장도 하지 않음)**

- N1. **작가 동정(attribution)** — 위작 판별·작가 추정은 범위 밖이다. 오판의 법적·시장적 파급이 크므로 MVP에서 다루지 않는다.
- N2. **가격·가치 평가** — 후속 K-VALUE 과제로 분리한다.
- N3. **진위·연대 판정** — 과학적 재료 분석 없이는 불가하다.
- N4. **최종 확정 메타데이터 생성** — 본 시스템의 산출물은 **초안(draft)**이며, 큐레이터 검수를 전제한다. 이 전제는 실무 수용성 설문 문항에도 그대로 반영한다.

> 비목표를 명시하는 것은 방어적 조치가 아니라 평가 설계의 필수 요건이다. 측정하지 않을 것을 정해야 측정할 것의 표본과 지표가 확정된다.

---

# 3. 시스템 아키텍처 및 계측 설계

## 3.1 4-Layer 분해의 근거

미술품을 단일 임베딩으로 압축하면 유사도가 무엇에서 비롯되었는지 분해할 수 없다. 본 시스템은 작품이 성립하는 미학적 조건에 대응하여 4개 층위로 특징을 분해한다.

| 층위 | 미학적 질문 | 실무 질의 예시 |
|---|---|---|
| 도상 (Iconography) | 무엇이 그려졌는가 | "산수 도상 작품만" |
| 기법 (Technique) | 어떻게 그려졌는가 | "이 필치와 유사한 붓질" |
| 물성 (Materiality) | 무엇으로 만들어졌는가 | "장지·분채 계열 작품" |
| 색채 (Color) | 어떤 색으로 구성되었는가 | "이 전시실 벽색과 충돌하지 않는 작품" |

## 3.2 레이어별 기술 사양

| 층위 | 주 추출기 | 보조/폴백 | 출력 형태 | 주요 실패 모드 | 실패 시 치명도 |
|---|---|---|---|---|---|
| 도상 | CLIP / SigLIP 임베딩 | VLM 텍스트 태깅 | 임베딩 + 태그 집합 | 한국 도상 어휘 부재 | 중 |
| 기법 | VGG 중간층 Gram 행렬 통계 | — | 스타일 벡터 + 태그 | 스캔 품질·조명 편차 민감 | 중 |
| 물성 | VLM (제약 어휘 강제) | 규칙 기반 분류 | 단일/다중 라벨 | 서양화 재료 편향 오인식 | **상** |
| 색채 | HSV 히스토그램 + k-means | 규칙 기반 색군 매핑 | 대표색 팔레트 + 색군 라벨 | 액자·배경 혼입 | **상** |

**치명도가 상인 층위(물성·색채)에만 규칙 기반 폴백을 두는 설계 근거:** 이 두 층위는 (a) 결정론적 신호(픽셀 통계, 재료 어휘)로 상당 부분 복원 가능하고, (b) 오류 시 DB 적재 후 하류 전파 비용이 가장 크다. 반면 도상·기법은 규칙 기반 대체가 사실상 불가능하므로, 실패 시 값을 지어내지 않고 `null` + `low_confidence` 플래그를 반환한다.

> **설계 원칙:** 모르는 것을 채워 넣는 것보다 모른다고 표시하는 것이 미술관 DB에서는 항상 낫다. 이 원칙은 `abstention`(판단 유보) 필드로 스키마에 명시된다.

## 3.3 Fail-safe 설계와 검증 방법 — 감사 항목 #8

v1.0의 "출력이 0이 되는 상황을 구조적으로 차단했습니다"는 **장애 주입 시험 없이는 성립하지 않는 주장**이다. 다음 시험을 설계한다.

| 주입 장애 유형 | 주입 방법 | 기대 동작 | 합격 기준 |
|---|---|---|---|
| VLM 타임아웃 | 응답 지연 강제 (>T_max) | 규칙 기반 폴백 경로 진입 | 색채·물성 필드 non-null 100% |
| VLM 5xx 오류 | 오류 응답 주입 | 1회 재시도 후 폴백 | 파이프라인 미중단 100% |
| 스키마 위반 JSON | 잘못된 키/타입 반환 | 재시도 1회 → 폴백 | 최종 스키마 유효 100% |
| 손상 이미지 | 잘린 파일·0바이트 | 전처리 단계 거부 + 오류코드 | `error_code` 기록 100% |
| 인덱스 미로드 | FAISS 인덱스 부재 | 검색만 실패, 4-Layer JSON은 반환 | 부분 성공 응답 100% |

산출 로그: `fault_injection_log.csv` (주입 유형, 시행 횟수, 폴백 진입 여부, 최종 출력 유효성)

**시행 규모 권고:** 유형당 20회 이상. 20회 전량 성공 시 실패율의 95% 신뢰 상한은 약 14%이며, 50회 전량 성공 시 약 6%로 내려간다. "100% 차단"이라는 표현은 어떤 표본 크기로도 성립하지 않으므로, 발표에서는 **"주입 시험 __회에서 폴백 진입 실패 0건"**의 형태로 서술한다.

## 3.4 구조화 출력 제어

VLM 해설 생성 단계는 **자유 생성이 아니라 제약된 분류**로 축퇴시킨다.

1. 고정 JSON 스키마를 프롬프트에 주입 (부록 A)
2. 물성 필드는 제약 어휘 20종 중에서만 선택 허용 (부록 B)
3. 온도 0 고정, 최대 토큰 상한 지정
4. 산출 JSON을 스키마 검증 → 실패 시 오류 메시지 첨부 재시도 1회 → 재실패 시 규칙 기반 폴백
5. 프롬프트·스키마·모델 버전을 `predictions.jsonl`에 전량 기록

> **지표 간 상충 주의:** JSON 1차 통과율은 스키마를 느슨하게 할수록 올라간다. 따라서 **스키마 버전을 평가 시작 전에 동결**하고, 통과율과 스키마 버전을 항상 함께 보고한다. 스키마 버전 없이 보고된 통과율은 무의미하다.

## 3.5 검색 계층 — 4-Index 가중합

4개 층위의 임베딩을 각각 독립 FAISS 인덱스로 구축하고, 검색 시 가중합한다.

```
score(q, d) = w_ico · sim_ico(q,d)
            + w_tec · sim_tec(q,d)
            + w_mat · sim_mat(q,d)
            + w_col · sim_col(q,d)

제약: w_ico + w_tec + w_mat + w_col = 1,  w_* >= 0
```

**축별 프리셋 (평가 시작 전 동결 필요)**

| 프리셋 | w_ico | w_tec | w_mat | w_col | 용도 |
|---|---:|---:|---:|---:|---|
| 균형 (default) | 0.40 | 0.20 | 0.20 | 0.20 | 일반 유사작 탐색 |
| 도상 단일 | 1.00 | 0.00 | 0.00 | 0.00 | 주제 기준 검색 |
| 물성 가중 | 0.15 | 0.15 | 0.55 | 0.15 | 재료 기준 검색 (데모 핵심 기능) |
| 색채 가중 | 0.15 | 0.15 | 0.15 | 0.55 | 전시 공간 색 조율 |

**동결 대상 파라미터:** 인덱스 타입(Flat/IVF/HNSW), 유사도(코사인/내적), 정규화 여부, `nlist`/`efSearch`, 가중치 프리셋 값, 코퍼스 스냅샷 해시. 이 중 하나라도 평가 중에 변경되면 그 이전 로그는 폐기하고 재실행한다.

> **주의:** 가중치를 평가 결과를 본 뒤에 조정하면 그 시점부터 골드셋은 검증셋이 아니라 **개발셋**이 된다. 튜닝이 필요하면 별도 개발셋(dev set)을 분리하고, 최종 판정은 한 번도 보지 않은 홀드아웃에서만 수행한다.

## 3.6 파이프라인 및 계측 지점 (Instrumentation Point)

지연시간 로그의 컬럼은 임의로 정한 것이 아니라, 아래 각 단계의 종료 시점에 대응한다.

| 순서 | 단계 | 처리 내용 | 계측 타임스탬프 |
|---:|---|---|---|
| 0 | 업로드 수신 | 이미지 수신, 요청 ID 발급 | `upload_started_at` |
| 1 | 정규화 | 장변 384px 리사이즈, pHash 중복 제거, EXIF 제거 | `image_normalized_at` |
| 2 | 병렬 추출 | 4-Layer 특징 독립 추출 | `feature_extracted_at` |
| 3 | 해설·검증 | VLM 해설 생성 → 스키마 검증 → (재시도/폴백) | `schema_validated_at` |
| 4 | 검색 | 4-Index 가중합 Top-5 산출 | `search_completed_at` |
| 5 | 렌더 | 화면 카드 4장 렌더링 완료 | `card_render_done_at` |
| 6 | 내보내기 | 4-Layer JSON, 한국어 해설, Top-5, CSV | (별도 계측) |

**총 처리시간 정의:** `total = card_render_done_at − upload_started_at`. 단계별 소요는 인접 타임스탬프 차분으로 얻으며, 병목 구간 식별에 사용한다. 모든 시각은 **단조 시계(monotonic clock)** 기준으로 기록하고 밀리초 단위로 저장한다.

## 3.7 재현성 고정 항목 (Reproducibility Lock)

평가 실행 전 다음을 `run_config.yaml`에 고정하고 해시를 기록한다.

| 범주 | 고정 항목 |
|---|---|
| 모델 | VLM 모델명·버전, CLIP/SigLIP 체크포인트, VGG 가중치 출처 |
| 생성 | temperature=0, top_p, max_tokens, 재시도 횟수(=1) |
| 프롬프트 | `prompt_version`, 제약 어휘 목록 파일 해시 |
| 전처리 | 리사이즈 규격(장변 384px), 보간법, pHash 임계값, 색공간 |
| 검색 | 인덱스 타입·파라미터, 가중치 프리셋, 코퍼스 스냅샷 해시 |
| 실행 환경 | OS, Python·라이브러리 버전, GPU/CPU 사양, 배치 크기, 동시 요청 수 |
| 난수 | 전역 시드, k-means 시드 |

> 재현성 패키지가 없으면 성능 수치는 "그날 그 노트북에서 그랬다"는 일화에 머문다. 본 프로젝트가 후속 K-VALUE의 **입력 피처 공급원**이 될 예정이라는 점에서, 재현성은 선택이 아니라 전제다.
---

# 4. 평가 설계 (Evaluation Protocol)

> 본 절이 v2.0 보고서의 핵심이다. v1.0에서 "평가 방법"은 한 페이지 표였으나, 그 표만으로는 어떤 결과가 나와도 사후에 해석을 바꿀 수 있었다. 아래 설계는 **결과가 나오기 전에 해석을 고정**하기 위한 것이다.

## 4.1 평가 철학

단일 정확도 하나로 시스템을 요약하지 않는다. 이유는 두 가지다.

**첫째, 층위별 난이도가 다르다.** 색채는 픽셀 통계에서 거의 결정론적으로 얻어지지만 도상은 문화적 해석이 개입한다. 이를 평균 내면 **색채의 높은 점수가 도상의 실패를 은폐한다.** 그래서 목표 기준을 색채 90 / 물성 80 / 기법 65 / 도상 55로 차등화하고, 평균 지표를 만들지 않는다.

**둘째, 정확도가 높아도 쓰이지 않는 시스템이 있다.** 15초 안에 안 나오면, JSON이 깨지면, 유사작 추천이 엉뚱하면, 큐레이터는 정확도와 무관하게 사용을 중단한다. 그래서 **유용성(정확도)·안정성(지연·스키마)·수용성(사람 판단)** 세 축을 분리해 각각 판정한다.

## 4.2 구성 타당도 매핑 (Construct Validity)

각 지표가 실제로 무엇을 대리하는지, 그리고 그 대리에 어떤 한계가 있는지를 명시한다.

| 실무 질문 | 지표 | 대리 관계의 한계 |
|---|---|---|
| 이 라벨을 그대로 DB에 넣어도 되는가 | 4-Layer 필드 단위 일치율 | 골드셋 작성자 1인의 판단이 곧 정답이라는 가정. 라벨러 간 신뢰도로 보완 |
| 실무 흐름을 끊지 않는가 | P95 처리시간 | 체감 지연은 P95 외에 분산·예측 가능성에도 좌우됨. P50/P99 병기 |
| DB에 자동 적재 가능한가 | JSON 1차 통과율 | 스키마 통과 ≠ 내용 정확. 일치율과 반드시 함께 읽어야 함 |
| 추천이 쓸 만한가 | Top-5 관련성 | 관련성 ≠ 유용성. 이미 아는 작품만 추천되면 관련성은 높아도 무용 |
| 실제로 쓸 것인가 | 리커트 평균 | 의향 ≠ 행동. n=5는 통계적 판정 불가 |

> **경고:** JSON 1차 통과율이 높고 일치율이 낮은 상태는 "형식은 완벽한데 내용이 틀린 데이터를 대량 생산하는 시스템"을 의미한다. 이는 아무것도 생산하지 않는 것보다 위험하다. 두 지표는 항상 쌍으로 보고한다.

## 4.3 1차 / 2차 종점 사전 지정

8개 지표를 동시에 유의수준 5%로 판정하면 최소 1개가 우연히 통과할 확률(FWER)은 `1 − 0.95^8 ≈ 33.7%`다. 따라서 종점을 위계화한다.

| 구분 | 지표 | 판정 유의수준 | 지정 사유 |
|---|---|---|---|
| **1차 종점** | 물성 일치율 | Bonferroni 보정 α = 0.05/3 ≈ 0.0167 | 본 시스템의 핵심 차별점이자 KPT에서 확인된 최대 약점 |
| **1차 종점** | P95 처리시간 | 동일 | 사용자와의 명시적 서비스 약속 |
| **1차 종점** | JSON 1차 통과율 | 동일 | DB 적재 가능성의 필요조건 |
| 2차 종점 | 색채·기법·도상 일치율 | 보정 없이 CI만 보고 | 탐색적 지표. 단독 성패 주장 금지 |
| 2차 종점 | Top-5 관련성 | 동일 | 탐색적 |
| 2차 종점 | 실무자 리커트 | 판정 없음 | n=5, 기술통계만 |

> 1차 종점을 3개로 좁히는 것은 겸손이 아니라 정밀도의 문제다. 모든 것을 주장하면 아무것도 주장하지 못한다.

## 4.4 골드셋 설계

### 4.4.1 표본 프레임 및 층화

무작위 추출이 아니라 **층화 추출(stratified sampling)**을 사용한다. 4-Layer 중 물성·기법의 난이도가 재료 계열에 따라 급격히 달라지기 때문이다.

| 층화 변수 | 구간 | 최소 배분 |
|---|---|---|
| 재료 계열 | 지본(장지·순지·화선지) / 견본 / 유화·아크릴 / 입체(목·석·금속) / 사진·판화 | 각 15% 이상 |
| 제작 시기 | 근대(~1945) / 현대(1945~2000) / 동시대(2000~) | 각 20% 이상 |
| 색채 특성 | 저채도 수묵 계열 / 고채도 채색 계열 | 각 30% 이상 |
| 도상 유형 | 구상 / 반추상 / 추상 | 각 20% 이상 |

**층화의 실무적 이유:** 무작위 추출 시 데이터 가용성이 높은 유화·현대·구상 작품에 표본이 쏠리고, 정작 이 시스템의 존재 이유인 한국화 재료 계열이 과소 표집된다. 그 상태의 물성 일치율 80%는 "한국화를 잘 맞힌다"는 근거가 되지 못한다.

### 4.4.2 표본 수 N 산정 — v1.0의 "100점"은 충분한가

정규 근사 기준 필요 표본 수는 `n = z² · p(1−p) / e²` (z=1.96)이며, 역으로 주어진 N에서의 신뢰구간 반폭은 `e = 1.96 · sqrt(p(1−p)/N)`이다.

**목표 기준별 필요 표본 수 (95% 신뢰, 반폭 e)**

| 목표 p | e=±10%p | e=±8%p | e=±7%p | e=±5%p |
|---:|---:|---:|---:|---:|
| 색채 0.90 | 35 | 55 | 71 | 139 |
| 물성 0.80 | 62 | 97 | 126 | 246 |
| 기법 0.65 | 88 | 137 | 179 | 350 |
| 도상 0.55 | 96 | 149 | 195 | 381 |

*(표본 수는 항상 올림 처리. 계산 검증은 `compute_metrics.py --selftest`로 재현 가능)*

**주어진 N에서의 신뢰구간 반폭**

| N | 색채(0.90) | 물성(0.80) | 기법(0.65) | 도상(0.55) |
|---:|---:|---:|---:|---:|
| **100 (v1.0 주장)** | ±5.9%p | ±7.8%p | ±9.4%p | **±9.8%p** |
| 150 (권고) | ±4.8%p | ±6.4%p | ±7.6%p | ±8.0%p |
| 200 (충분) | ±4.2%p | ±5.5%p | ±6.6%p | ±6.9%p |

> **핵심 지적:** N=100에서 도상 일치율의 신뢰구간 반폭은 약 ±10%p다. 즉 관측값이 55%로 나와도 참값은 45~65% 어디든 가능하며, **목표 55% 달성 여부를 판정할 수 없다.** v1.0이 "골드셋 100점으로 측정했다"고 서술한 것은 표본 설계 없이 정한 수치일 가능성이 높다.

**최종 권고:** 
- **본 실험 N = 150** (MVP 공수 대비 현실적 상한, 도상 반폭 ±8%p)
- 층화 배분: 재료 계열 5구간 × 30점
- 파일럿 N = 20 (본 실험 골드셋과 **중복 없이** 별도 구성, 채점 스크립트·라벨 지침 검증용)
- 예산 제약으로 N < 100이 불가피하면, 결과표에 **"판정 불가(표본 부족)"**를 명시하고 점추정만 참고치로 제시한다.

### 4.4.3 라벨링 프로토콜 및 라벨러 간 신뢰도

골드셋의 품질이 곧 평가의 상한이다. 라벨러가 서로 다른 답을 내는 필드에서는 모델이 아무리 잘해도 일치율이 오르지 않는다.

| 절차 | 내용 |
|---|---|
| 라벨러 | 큐레이터 자격 보유자 2인 (`curator_id` 기록) |
| 라벨 지침 | 층위별 정의·허용 어휘·경계 사례 문서화 후 배포 (부록 C) |
| 이중 라벨링 | 전체의 30% 이상(N=150이면 45점)을 두 라벨러가 독립 라벨링 |
| 신뢰도 산출 | 층위별 Krippendorff α (다중 라벨 대응) 및 Cohen κ 병기 |
| 합격 기준 | α ≥ 0.80 수용, 0.667 ≤ α < 0.80 잠정, α < 0.667 **지침 개정 후 재라벨링** |
| 불일치 조정 | 제3자 조정(adjudication) 또는 합의 회의로 최종 골드 라벨 확정, 조정 이력 기록 |
| 블라인드 | 라벨러는 모델 출력을 보지 않은 상태에서 라벨링 (앵커링 차단) |

> **필수 보고 항목:** 각 층위의 모델 일치율 옆에 반드시 **인간 라벨러 간 일치율**을 병기한다. 도상 일치율 모델 55% / 인간 60%라면 이는 "모델이 못한다"가 아니라 "그 층위는 원래 어렵다"는 뜻이며, 해석이 완전히 달라진다. 인간 상한(human ceiling) 없이 보고된 모델 정확도는 평가되지 않은 수치다.

### 4.4.4 데이터 누수 차단

| 누수 경로 | 차단 조치 |
|---|---|
| 골드셋 작품이 검색 인덱스에 포함 | 골드셋 `artwork_id`를 인덱스 코퍼스에서 제외, 또는 검색 시 자기 자신 제외 규칙 명시 |
| 동일 작품의 다른 촬영본 중복 | pHash 임계값 기반 근사 중복 제거를 **골드셋 구성 단계에서 선행** |
| 프롬프트 예시로 골드셋 작품 사용 | few-shot 예시는 골드셋 밖에서만 선정 |
| 튜닝 중 골드셋 반복 참조 | 개발셋 분리, 골드셋은 최종 1회만 실행 (실행 횟수 로그 기록) |

## 4.5 채점 규칙 (Matching Rule) — "일치"의 정의

v1.0은 "필드 단위 일치율"이라고만 했으나, **무엇을 일치로 볼 것인지가 정의되지 않으면 같은 로그로 전혀 다른 수치가 나온다.** 다음을 사전 확정한다.

### 4.5.1 정규화 규칙 (모든 층위 공통)

1. 유니코드 NFC 정규화, 좌우 공백 제거
2. 대소문자 통일, 중복 공백 축약
3. 괄호 주석 제거 (예: `장지에 분채(부분 금박)` → `장지에 분채`)
4. 조사·접미 표현 제거 (`~계열`, `~풍`, `~로 추정`)
5. 동의어 사전 적용 (`한지 = 장지 = 순지` 여부는 사전에서 **명시적으로 결정**하고 그 결정을 문서화)

> 동의어 사전은 평가 전에 동결한다. 결과를 본 뒤 동의어를 추가하는 것은 점수를 조작하는 것과 구별되지 않는다. 사전 파일: `synonym_map.csv` (해시 기록).

### 4.5.2 층위별 채점 방식

| 층위 | 라벨 구조 | 채점 방식 | 부분 점수 |
|---|---|---|---|
| 색채 | 다중 라벨 (대표 색군 3개) | 집합 일치 — Jaccard ≥ 0.5를 일치로 판정 | 있음 (보조 지표로 micro-F1 병기) |
| 물성 | 단일 주라벨 + 보조 라벨 | **주라벨 완전 일치만 일치로 인정** | 없음 (보수적 채점) |
| 기법 | 다중 라벨 | 상위 1개 라벨 일치 여부 | 보조로 micro-F1 |
| 도상 | 다중 라벨 (주제 태그) | 골드 태그 중 1개 이상 포함 시 일치 | 보조로 micro-F1 |

**주 지표는 위 정의에 따른 이진 일치율(binary match rate)로 고정한다.** micro-F1·Jaccard는 보조 지표로만 보고하며, 주 지표를 대체하지 않는다.

### 4.5.3 집계 방식

- **micro (기본):** 전체 평가 건수 기준 일치 비율. 결과표의 주 수치.
- **macro (병기):** 층화 구간별 일치율의 단순 평균. 소수 계열(예: 견본, 입체)의 성능 저하를 드러낸다.
- micro와 macro의 격차가 10%p 이상이면 특정 계열에 성능이 쏠려 있다는 신호이며, 반드시 서브그룹 표를 함께 제시한다.

### 4.5.4 판단 유보(abstention)의 처리

모델이 `null` + `low_confidence`를 반환한 건은 **불일치로 계산한다(보수적 처리).** 동시에 `유보율 = 유보 건수 / 전체 건수`를 별도 보고한다. 유보율이 높은데 일치율도 높다면 이는 "쉬운 것만 답하는" 전략일 수 있으므로, **커버리지-정확도 곡선**을 부록에 첨부한다.

## 4.6 판정 규칙 (Decision Rule) — 달성 / 미달 / 보류의 통계적 정의

보완안이 도입한 `달성/미달/보류` 3분 판정은 방향은 옳으나 기준이 없다. 다음과 같이 **Wilson 점수 구간**으로 정의한다. (Wilson 구간은 소표본·극단 비율에서 정규 근사보다 안정적이며, 90%대 목표치를 다루는 본 과제에 적합하다.)

**비율 지표 (일치율, JSON 통과율, Top-5 관련성)**

```
Wilson 95% 신뢰구간:
  center = (p̂ + z²/2n) / (1 + z²/n)
  half   = z · sqrt( p̂(1−p̂)/n + z²/4n² ) / (1 + z²/n)
  CI     = [center − half, center + half],  z = 1.96 (1차 종점은 z = 2.394, Bonferroni)

판정:
  달성  ← CI 하한 ≥ 목표치
  미달  ← CI 상한 <  목표치
  보류  ← 그 외 (구간이 목표치를 포함)
```

**지연시간 지표 (P95)**

```
P95 추정: 정렬 후 선형보간 백분위수
불확실성: 부트스트랩 2,000회 재표본 → P95의 95% 백분위 구간

판정:
  달성  ← 부트스트랩 CI 상한 ≤ 15초
  미달  ← 부트스트랩 CI 하한 >  15초
  보류  ← 그 외
```

**리커트 지표 (n=5)**

```
판정하지 않는다. 평균·중앙값·최솟값·개별 점수·정성 코멘트를 전량 제시한다.
사유: s=0.8 가정 시 n=5의 표준오차는 약 0.36, 95% CI는 약 ±0.99.
     즉 평균 3.8이 나와도 참값은 2.8~4.8 구간이며 3.5 기준을 가를 수 없다.
```

> **이 판정 규칙이 v1.0 대비 갖는 의미:** 점추정만으로 판정하면 물성 79%는 "미달", 81%는 "달성"이 된다. 그러나 N=150에서 두 값의 신뢰구간은 거의 완전히 겹친다. 신뢰구간 기반 판정은 **"아직 모른다"를 정직하게 말할 수 있는 언어**를 제공하며, 이것이 본 보고서 전체의 태도와 일치한다.

### 4.6.1 판정 임계 카운트 — 데이터를 보기 전에 확정하는 합격선

판정 규칙을 산식으로만 남기면, 실측값이 나온 뒤 "이 정도면 달성 아니냐"는 사후 협상이 반드시 발생한다. 이를 원천 차단하기 위해 **각 지표가 '달성'으로 판정되기 위한 최소 관측 건수를 데이터 수집 전에 계산해 고정한다.**

**표본 N별 '달성' 판정 최소 관측 건수 (Wilson 하한 ≥ 목표치)**

| 지표 | 종점 | 목표 | N=100 | N=150 (권고) | N=200 |
|---------------------|:----:|-----:|------------:|------------:|------------:|
| 색채 일치율 | 2차 | 90% | 96 (96.0%) | 143 (95.3%) | 189 (94.5%) |
| **물성 일치율** | **1차** | 80% | 90 (90.0%) | 132 (88.0%) | 174 (87.0%) |
| 기법 일치율 | 2차 | 65% | 75 (75.0%) | 109 (72.7%) | 144 (72.0%) |
| 도상 일치율 | 2차 | 55% | 65 (65.0%) | 95 (63.3%) | 124 (62.0%) |
| **JSON 1차 통과율** | **1차** | 95% | **판정 불가** | 149 (99.3%) | 198 (99.0%) |
| Top-5 관련성 | 2차 | 60% | 70 (70.0%) | 102 (68.0%) | 134 (67.0%) |

*1차 종점은 Bonferroni 보정 z=2.394, 2차 종점은 z=1.96 적용.*

이 표에서 세 가지 사실이 즉시 드러난다.

**(1) 목표 기준과 합격선은 다르다.** 색채 목표는 90%지만, N=150에서 실제로 '달성'을 선언하려면 관측 일치율이 **95.3%** 여야 한다. 92%가 나오면 판정은 '보류'다. 이 간극을 사전에 인지하지 못하면, 발표 당일 "92%로 목표 90%를 상회했습니다"라는 통계적으로 잘못된 문장이 나온다.

**(2) JSON 1차 통과율 95% 목표는 N=100에서 원리적으로 판정할 수 없다.** 100건 전부가 통과해도 Bonferroni 보정 하의 Wilson 하한이 95%에 미치지 못한다. 목표치가 표본 규모의 정밀도 한계에 걸려 있기 때문이다.

| 관측 1차 통과율 | '달성' 판정에 필요한 최소 N |
|---:|---:|
| 100% | 109 |
| 99% | 200 |
| 98% | 350 |
| 97% | 700 |

> **실행 권고:** 스키마 검증과 지연시간 측정은 **큐레이터 인건비가 전혀 들지 않는 완전 자동 로그**다. 골드셋 라벨링은 30시간이 들지만, 이 두 로그는 배치 실행만 걸어두면 N=300~500을 사실상 무비용으로 확보할 수 있다. **비싼 지표는 N을 아끼고, 싼 지표는 N을 키운다** — 이것이 제한된 공수에서 판정 가능한 주장을 최대화하는 유일한 방법이다. 따라서 `schema_validation_log.csv`와 `latency_log.csv`의 목표 표본은 **N ≥ 300**으로 상향한다.

**(3) 도상 55%는 N=100에서 65% 이상이어야 달성이다.** 목표치가 0.5 근처일수록 이항 분산 `p(1−p)`가 최대가 되어 구간이 가장 넓어진다. 목표를 낮게 잡는 것이 판정을 쉽게 만들지 않는다는 점을 유의해야 한다.

**판정 임계값의 동결:** 위 표는 데이터 수집 개시 전에 `protocol_v2.0.md`에 포함하여 해시를 기록한다. 실측 후 임계값을 조정하는 것은 사전등록 위반으로 간주하고, 조정이 불가피할 경우 **조정 사실·사유·조정 전후 값을 결과표에 명시**한다.

## 4.7 지연시간 측정 프로토콜

| 항목 | 규정 |
|---|---|
| 표본 수 | 성공 실행 **N ≥ 300** (최소선 100). 인건비가 들지 않는 자동 로그이므로 표본을 아낄 이유가 없다 (§4.6.1) |
| 워밍업 | 최초 5회 실행은 콜드 스타트로 간주하여 **제외**하고, 제외 사실을 명시 |
| 실패 처리 | 실패 실행은 P95 계산에서 제외하되 **실패율을 별도 보고** (실패를 빼서 지연이 좋아 보이는 착시 차단) |
| 동시성 | 단일 요청(concurrency=1) 조건을 기본으로 하고, 동시 3·5 조건을 부가 측정 |
| 보고 항목 | P50 / P90 / P95 / P99 / 최댓값 / 실패율 / 단계별 소요 분해 |
| 환경 명시 | 하드웨어, 네트워크 조건, 이미지 평균 용량, 측정 일시 |

> **표본 수의 통계적 근거:** 모든 표본이 참 P95 미만일 확률은 `0.95^n`이다. n=59에서 이 값이 5% 아래로 내려가므로, **n ≥ 59는 P95 추정의 최소선**이고 안정적 추정에는 100 이상이 필요하다. N=20 수준의 로그로 산출한 P95는 보고하지 않는다.

## 4.8 JSON 스키마 안정성 프로토콜

| 항목 | 규정 |
|---|---|
| **표본 수** | **N ≥ 300 필수.** 목표 95%는 N=100에서 만점이어도 판정 불가하다(§4.6.1). 자동 배치 실행으로 확보 |
| 1차 통과 | **재시도·폴백을 적용하기 전** 최초 생성물이 스키마 검증을 통과한 경우만 인정 |
| 최종 통과 | 재시도·폴백 후 최종 유효 여부. 별도 컬럼(`final_valid`)으로 기록 |
| 병기 필수 | 1차 통과율 · 최종 통과율 · 폴백 사용률 3개를 항상 함께 보고 |
| 실패 분류 | 필수 필드 누락 / 타입 불일치 / 허용값 위반 / 파싱 실패 로 구분 기록 |
| 스키마 버전 | 통과율 수치 옆에 `schema_version`을 반드시 병기 |

> 1차 통과율 95%와 폴백 사용률 40%가 동시에 성립할 수 없음에 유의한다. 두 수치의 정합성 검사는 채점 스크립트에 자동 검증으로 포함한다.

## 4.9 검색 품질 평가 프로토콜

| 항목 | 규정 |
|---|---|
| 쿼리 수 | 축당 **30개 이상** (정보검색 평가 관례: 25~50 토픽) |
| 평가 축 | MVP는 도상 단일 / 물성 가중 2개 축을 필수, 균형·색채 축은 선택 |
| 판정 규모 | 30 쿼리 × 2축 × Top-5 = **300건** |
| 척도 | 0 = 무관 / 1 = 약한 관련 / 2 = 명확히 관련 |
| 주 지표 | **보수적 관련성 = (2점 건수) / (전체 판정 건수) × 100** |
| 보조 지표 | 완화 관련성(1점 이상), P@5, nDCG@5 |
| 이중 평가 | 전체의 30%를 2인 평가하여 α 산출 |
| 블라인드 | 평가자에게 검색 축·순위를 숨기고 결과를 무작위 배열하여 제시 |

**보고 규칙:** 관련성 비율을 제시할 때는 표 하단에 판정 기준을 반드시 명시한다. "관련 판정 기준: 2점만" 또는 "1점 이상". 기준을 밝히지 않은 관련성 수치는 비교 불가능하다.

**nDCG@5 산식 (보조)**

```
DCG@5  = Σ_{i=1..5} (2^rel_i − 1) / log2(i + 1)
IDCG@5 = 판정 풀을 관련도 내림차순 정렬했을 때의 DCG@5
nDCG@5 = DCG@5 / IDCG@5
```

> nDCG를 보조로 두는 이유: 관련성 비율은 순위를 무시한다. 1위가 무관이고 5위가 관련인 경우와 그 반대가 같은 점수를 받는데, 실무에서는 전혀 다른 경험이다.

## 4.10 실무 수용성 평가 프로토콜

| 항목 | 규정 |
|---|---|
| 응답자 | 미술관·갤러리 실무자 5인 (소속·경력 구간 기록, 개인식별정보 비수집) |
| 블라인드 조건 | AI 산출물임을 밝히지 않고 제시. `blind_condition` 컬럼에 조건 코드 기록 |
| 대조 제시 | 가능하면 수기 초안과 AI 초안을 무작위 순서로 병렬 제시(A/B) |
| 핵심 문항 | "이 초안을 **검수해서** 실무에 사용할 의향이 있는가" (1~5) |
| 보조 문항 | 신뢰도, 수정 소요 예상시간, 가장 신뢰 못 할 층위(선택형) |
| 보고 방식 | 평균 + 중앙값 + 개별 5개 점수 전량 + 정성 코멘트 주제 코딩 |
| **금지** | n=5로 "검증했다"·"달성했다"고 말하지 않는다 |

> **핵심 문항 설계의 주의점:** "검수해서"라는 단어를 반드시 포함해야 한다. 이 단어가 빠지면 응답자는 '완성본으로 쓸 것인가'를 묻는 것으로 이해하고 점수가 구조적으로 낮아진다. 본 시스템의 비목표 N4(초안 생성)와 문항이 일치해야 한다.

## 4.11 서브그룹 및 편향 분석

전체 평균은 특정 집단에서의 실패를 은폐한다. 다음 분해를 필수 산출물로 규정한다.

| 분해 축 | 보고 지표 | 확인 목적 |
|---|---|---|
| 재료 계열 (5구간) | 층위별 일치율 + N + CI | 한국화 재료에서의 성능 하락 여부 (KPT Problem 검증) |
| 제작 시기 (3구간) | 동일 | 근대 작품 상태(변색·손상)의 영향 |
| 색채 특성 (2구간) | 색채·물성 일치율 | 저채도 수묵 계열에서의 색군 오분류 |
| 이미지 품질 | 일치율 | 스캔·촬영 품질이 성능에 미치는 영향 |

**최악 그룹 성능(worst-group accuracy)**을 별도 보고한다. 전체 물성 일치율 80%인데 견본 계열만 45%라면, 그 시스템은 견본 소장 기관에서는 쓸 수 없다. 서브그룹 N이 20 미만이면 CI가 매우 넓으므로 **참고치로만 제시**하고 판정하지 않는다.

## 4.12 절제 실험 (Ablation) — 감사 항목 #7 검증 설계

v1.0의 **"제약 어휘 주입으로 물성 정확도를 10%p 이상 끌어올렸다"**는 인과 주장이다. 인과 주장에는 대조군이 필요하다.

| 조건 | 프롬프트 | 그 외 조건 |
|---|---|---|
| A (대조) | 제약 어휘 **없음**, 자유 생성 | 동일 골드셋, 동일 모델·시드·전처리 |
| B (처치) | 제약 어휘 20종 주입 | 동일 |

```
개선폭 Δ = 물성 일치율(B) − 물성 일치율(A)   [단위: %p]

불확실성: 동일 표본에 대한 대응 비교이므로 McNemar 검정 또는
         쌍대 부트스트랩(2,000회)으로 Δ의 95% CI를 산출한다.

판정: CI 하한 > 0 → 개선 효과 확인
     CI가 0을 포함 → "개선 방향이 관측되었으나 유의하지 않음"
```

**보고 형식 (예시):** `Δ = +__%p (95% CI __ ~ __, N=__, McNemar p=__)`

> 절제 실험 없이 "+10%p"를 발표에 넣는 것은 본 보고서의 가장 큰 잠재 실점 요인이다. 심사자가 "그 10%p는 어떻게 쟀습니까"라고 물으면 답할 수 없다. 현 판본에서는 해당 수치를 **삭제**하고, 위 설계를 제시하는 것으로 대체한다.

## 4.13 오류 분석 분류 체계

정량 지표만으로는 다음 개선 방향이 나오지 않는다. 불일치 건 전량에 오류 유형을 부여한다.

| 코드 | 오류 유형 | 예시 | 대응 방향 |
|---|---|---|---|
| E-MAT-01 | 서양화 재료 편향 | 장지·분채 → '아크릴' | 제약 어휘 강화 |
| E-MAT-02 | 지종 세분 실패 | 장지/순지/화선지 미구분 | 동의어 사전 정책 결정 |
| E-COL-01 | 액자·배경 혼입 | 액자색이 대표색에 포함 | 크롭·세그멘테이션 전처리 |
| E-COL-02 | 저채도 색군 오분류 | 수묵 담채 → '무채색' | 색군 경계 재정의 |
| E-TEC-01 | 촬영 조건 민감 | 조명 반사로 필치 왜곡 | 정규화 강화 |
| E-ICO-01 | 한국 도상 어휘 부재 | 십장생·문자도 미인식 | 도상 어휘집 구축 |
| E-SYS-01 | 스키마 위반 | 허용값 밖 문자열 | 프롬프트·검증 강화 |
| E-SYS-02 | 폴백 오작동 | 폴백 진입 실패 | 장애 처리 로직 수정 |

산출물: `error_taxonomy.csv` — 유형별 건수 상위 3개를 발표에서 언급하고, 각각에 대응하는 다음 스프린트 액션을 1:1로 붙인다.

## 4.14 사전등록 및 데이터 잠금 절차

| 순서 | 절차 | 산출물 |
|---:|---|---|
| 1 | 본 v2.0 문서를 평가 프로토콜로 확정, 해시 기록 | `protocol_v2.0.md.sha256` |
| 2 | 골드셋 라벨링 완료 후 **파일 동결**, 이후 수정 금지 | `gold_labels.csv` + 해시 |
| 3 | 채점 스크립트를 **골드셋 실행 전에** 작성·검증 (더미 데이터로) | `compute_metrics.py` |
| 4 | `run_config.yaml` 고정 후 본 실행 1회 | `predictions.jsonl` 등 로그 9종 |
| 5 | 채점 스크립트 실행, 결과 자동 산출 (수기 계산 금지) | `results_v3.0.md` |
| 6 | 결과를 본 뒤 기준·가중치·사전을 수정하지 않는다 | 변경 시 사유·일시 기록 필수 |

> 3번이 가장 중요하다. **데이터를 보기 전에 분석 코드를 완성하는 것**이 사후 기준 이동을 막는 가장 확실한 장치다. 본 보고서와 함께 채점 스크립트 초안을 첨부하는 이유가 여기에 있다.
---

# 5. [발표용 슬라이드 삽입 1] 평가 결과 요약

## 5.1 결과표 (실측값 미기입 — 원시 데이터 확보 후 입력)

본 프로젝트는 K-ART LENS가 실제 미술관·갤러리 DB 입력 업무에 사용 가능한지를 확인하기 위해, **4-Layer 라벨 일치율 · 처리시간 · JSON 안정성 · 유사작 검색 품질 · 실무 수용성**을 분리해 평가하도록 설계했다. 아래 표에서 확인 가능한 것은 **측정 방법과 목표 기준**이며, 실측 결과값 산출에 필요한 골드셋·실행 로그·관련도 채점표·설문 원자료는 현재 확보되지 않았다. **근거 없는 수치는 임의로 기입하지 않는다.**

**표 A — 판정표 (발표 슬라이드에 그대로 삽입)**

| 지표 | 종점 | 목표 기준 | 실측 결과 | 95% CI | 판정 |
|-----------------------------|:----:|----------:|--------------------|--------------|--------------|
| 색채 일치율 | 2차 | 90% 이상 | `__ / __ = __%` | `[__, __]` | `달성/미달/보류` |
| **물성 일치율** | **1차** | 80% 이상 | `__ / __ = __%` | `[__, __]` | `달성/미달/보류` |
| 기법 일치율 | 2차 | 65% 이상 | `__ / __ = __%` | `[__, __]` | `달성/미달/보류` |
| 도상 일치율 | 2차 | 55% 이상 | `__ / __ = __%` | `[__, __]` | `달성/미달/보류` |
| **P95 처리시간** | **1차** | 15초 이내 | `__초` | `[__, __]` | `달성/미달/보류` |
| **JSON 1차 통과율** | **1차** | 95% 이상 | `__ / __ = __%` | `[__, __]` | `달성/미달/보류` |
| 유사작 Top-5 관련성 | 2차 | 60% 이상 | `__ / __ = __%` | `[__, __]` | `달성/미달/보류` |
| 실무자 리커트 평균 | 참고 | 3.5점 이상 | `평균 __ / 5.0, n=5` | 산출 안 함 | **판정 없음** |

**표 B — 지표별 근거 데이터 추적 (감사용, 슬라이드에는 생략 가능)**

| 지표 | 평가 구분 | 근거 원시 데이터 |
|---|---|---|
| 색채·물성·기법·도상 일치율 | 4-Layer 라벨 정확도 | `gold_labels.csv`, `predictions.jsonl`, `synonym_map.csv` |
| P95 처리시간 | 시스템 안정성 | `latency_log.csv` |
| JSON 1차 통과율 | 시스템 안정성 | `schema_validation_log.csv` |
| 유사작 Top-5 관련성 | 검색 품질 | `top5_relevance_eval.csv` |
| 실무자 리커트 평균 | 실무 수용성 | `practitioner_survey.csv` |
| (전 지표 공통) | 재현성 | `run_manifest.json` |

**표 하단 필수 주기 (발표 자료에 그대로 삽입)**

- 골드셋 N = `__`, 라벨러 `__`인, 층위별 라벨러 간 신뢰도 α = `__`
- Top-5 관련 판정 기준: `2점만 / 1점 이상` 중 택일 명시
- 지연시간 표본 N = `__` (콜드 스타트 `__`회 제외), 실패율 `__%`
- 스키마 버전 `__`, 프롬프트 버전 `__`, 모델 버전 `__`
- 실무자 리커트는 n=5로 통계적 판정 대상이 아니며 기술통계로만 제시

## 5.2 함께 제시할 인간 상한 대조표

| 층위 | 모델 일치율 | 인간 라벨러 간 일치율 | 격차 | 해석 |
|---|---|---|---|---|
| 색채 | `__%` | `__%` | `__%p` | |
| 물성 | `__%` | `__%` | `__%p` | |
| 기법 | `__%` | `__%` | `__%p` | |
| 도상 | `__%` | `__%` | `__%p` | |

## 5.3 발표 멘트 (슬라이드 1)

> "평가 항목은 실제 사용 가능성을 기준으로 설계했습니다. 4-Layer 라벨은 큐레이터 골드셋과의 필드 단위 일치율로 보고, 시스템 안정성은 P95 처리시간과 JSON 스키마 1차 통과율로 판단합니다. 검색 품질은 유사작 Top-5의 관련도 평가로, 현장 적용 가능성은 실무자 5명의 리커트 평가로 확인합니다.
>
> 여기서 한 가지를 분명히 말씀드리겠습니다. **이 표의 실측 칸이 비어 있는 것은 실수가 아니라 의도입니다.** 현재 제출 자료에는 골드셋 원본, 모델 출력 로그, 처리시간 로그, 관련도 채점표, 설문 원자료가 포함되어 있지 않습니다. 근거 로그 없이 산출된 수치는 감사에 통과하지 못하고, 미술관 소장품 DB처럼 오기록이 하류로 전파되는 도메인에서는 그 자체가 위험 자산이 됩니다.
>
> 대신 저는 **판정 규칙까지 미리 확정**했습니다. 8개 지표 중 1차 종점 3개를 지정했고, 신뢰구간 하한이 목표치를 넘을 때만 달성으로 판정합니다. 구간이 목표치를 걸치면 '보류'입니다. 데이터가 채워지는 순간 해석의 여지 없이 결론이 나오도록 설계했습니다."

---

# 6. [발표용 슬라이드 삽입 2] 측정식 및 결과 입력 형식

## 6.1 주 산식

| 지표 | 측정식 | 결과 입력 형식 예시 |
|---|---|---|
| 색채/물성/기법/도상 일치율 | `일치율(layer) = 일치 건수 / 평가 대상 건수 × 100` | `색채: 92 / 100 = 92.0%` |
| P95 처리시간 | `P95 = percentile_95(card_render_done_at − upload_started_at)` | `P95 처리시간: 12.8초` |
| JSON 스키마 1차 통과율 | `1차 통과율 = 1차 생성 JSON 중 스키마 유효 건수 / 전체 생성 건수 × 100` | `96 / 100 = 96.0%` |
| Top-5 관련성 | `관련성 = 관련 판정 항목 수 / 전체 Top-5 평가 항목 수 × 100` | `68 / 100 = 68.0%` |
| 실무 수용성 | `평균점수 = 리커트 점수 합 / 응답자 수` | `평균 3.8 / 5.0, n=5` |
| 물성 개선 효과 | `Δ = 제약어휘 적용 후 일치율 − 적용 전 일치율` | `+__%p (95% CI __ ~ __)` |

## 6.2 보조 산식 (판정·불확실성용)

| 용도 | 산식 |
|---|---|
| 비율의 95% 신뢰구간 (Wilson) | `center = (p̂ + z²/2n)/(1 + z²/n)`, `half = z·sqrt(p̂(1−p̂)/n + z²/4n²)/(1 + z²/n)` |
| 필요 표본 수 | `n = z²·p(1−p)/e²` |
| P95 불확실성 | 부트스트랩 2,000회 재표본 → P95 분포의 2.5 / 97.5 백분위 |
| 라벨러 간 신뢰도 | Krippendorff `α = 1 − D_o/D_e` (관측 불일치 / 기대 불일치) |
| 검색 순위 품질 | `nDCG@5 = DCG@5 / IDCG@5`, `DCG@5 = Σ (2^rel_i − 1)/log2(i+1)` |
| 개선 효과 검정 | 대응 표본 McNemar 검정 또는 쌍대 부트스트랩 |
| 시간 절감률 | `절감률 = (수기 중앙값 − 시스템 P50) / 수기 중앙값 × 100` |

## 6.3 Top-5 관련도 척도 및 산출 기준

| 점수 | 정의 | 판정 예시 |
|:---:|---|---|
| 0 | 무관 | 재료·주제·시대 어느 축에서도 연결점 없음 |
| 1 | 약한 관련 | 한 축에서만 유사, 실무에서 함께 제시하기 어려움 |
| 2 | 명확히 관련 | 큐레이터가 실제로 나란히 검토할 만함 |

**산출 기준:** 발표용 관련성 비율은 보수적으로 `2점 항목 수 / 전체 평가 항목 수 × 100`으로 산출한다. 1점까지 관련으로 포함하려면 표 하단에 **"관련 판정 기준: 1점 이상"**을 반드시 명시한다. 두 기준의 수치를 모두 산출해 병기하는 것을 권장한다 (`엄격 __% / 완화 __%`).

## 6.4 수치 표기 규칙

| 항목 | 규칙 |
|---|---|
| 유효숫자 | 비율은 소수점 첫째 자리까지 (`92.0%`), 시간은 소수점 첫째 자리까지 (`12.8초`) |
| 분자·분모 병기 | 모든 비율은 `분자 / 분모 = 비율` 형태로 표기. 비율 단독 표기 금지 |
| 표본 수 병기 | 모든 지표에 `N=__` 병기 |
| 반올림 | 은행가 반올림 사용 금지, 일반 반올림 |
| 계산 주체 | 모든 수치는 `compute_metrics.py` 출력에서 복사. **수기 계산·수기 입력 금지** |

---

# 7. 원시 데이터 및 로그 스키마 (Data Contract)

향후 성과 증빙 시 누락을 방지하기 위해 다음 데이터 확보를 파이프라인의 **필수 요건**으로 규정한다. 보완안의 6종에 3종을 추가하여 총 9종으로 확정한다.

## 7.1 파일 목록 및 책임

| # | 파일 | 생성 주체 | 시점 | 필수 |
|---:|---|---|---|:---:|
| 1 | `gold_labels.csv` | 큐레이터 | 실행 전 (동결) | 필수 |
| 2 | `predictions.jsonl` | 시스템 | 실행 중 | 필수 |
| 3 | `latency_log.csv` | 시스템 | 실행 중 | 필수 |
| 4 | `schema_validation_log.csv` | 시스템 | 실행 중 | 필수 |
| 5 | `top5_relevance_eval.csv` | 큐레이터 | 실행 후 | 필수 |
| 6 | `practitioner_survey.csv` | 실무자 | 실행 후 | 필수 |
| 7 | `annotator_agreement.csv` | 채점 스크립트 | 라벨링 후 | **추가** |
| 8 | `fault_injection_log.csv` | 시스템 | 별도 시험 | **추가** |
| 9 | `run_config.yaml` | 실행자 | 실행 전 (동결) | **추가** |

## 7.2 `gold_labels.csv`

| 컬럼명 | 타입 | 필수 | 설명 |
|---|---|:---:|---|
| `artwork_id` | string | O | 평가 대상 작품 ID (전 파일 조인 키) |
| `image_path` | string | O | 작품 이미지 경로 |
| `image_sha256` | string | O | 이미지 무결성 해시 (**추가 권고**) |
| `curator_id` | string | O | 골드 라벨 작성자 ID |
| `label_round` | int | O | 1차/2차 라벨링 회차 (이중 라벨링 식별, **추가 권고**) |
| `stratum_material` | string | O | 층화 구간: 재료 계열 (**추가 권고**) |
| `stratum_period` | string | O | 층화 구간: 제작 시기 (**추가 권고**) |
| `iconography_gold` | string | O | 도상 골드 라벨 (다중 시 `;` 구분) |
| `technique_gold` | string | O | 기법 골드 라벨 |
| `materiality_gold` | string | O | 물성 골드 라벨 (주라벨 우선 기재) |
| `color_gold` | string | O | 색채 골드 라벨 |
| `label_confidence` | int | △ | 라벨러 자신감 1~3 (**추가 권고**) |
| `adjudicated` | bool | △ | 불일치 조정 거친 건 여부 (**추가 권고**) |

## 7.3 `predictions.jsonl`

| 필드명 | 타입 | 필수 | 설명 |
|---|---|:---:|---|
| `run_id` | string | O | 실행 ID |
| `artwork_id` | string | O | 평가 대상 작품 ID |
| `iconography_pred` | string/array | O | 모델 도상 예측 |
| `technique_pred` | string/array | O | 모델 기법 예측 |
| `materiality_pred` | string | O | 모델 물성 예측 (주라벨) |
| `color_pred` | string/array | O | 모델 색채 예측 |
| `fallback_used` | bool | O | 규칙 기반 폴백 사용 여부 |
| `abstained_layers` | array | △ | 판단 유보한 층위 목록 (**추가 권고**) |
| `confidence` | object | △ | 층위별 신뢰도 점수 (**추가 권고**) |
| `prompt_version` | string | O | 프롬프트 버전 |
| `model_version` | string | O | 모델 버전 |
| `schema_version` | string | O | 스키마 버전 (**추가 권고**) |
| `raw_response` | string | △ | 모델 원문 응답 (오류 분석용, **추가 권고**) |

## 7.4 `latency_log.csv`

| 컬럼명 | 타입 | 필수 | 설명 |
|---|---|:---:|---|
| `run_id` | string | O | 실행 ID |
| `artwork_id` | string | O | 작품 ID |
| `upload_started_at` | timestamp(ms) | O | 업로드 시작 시각 |
| `image_normalized_at` | timestamp(ms) | O | 이미지 정규화 완료 |
| `feature_extracted_at` | timestamp(ms) | O | 4-Layer 추출 완료 |
| `schema_validated_at` | timestamp(ms) | O | JSON 검증 완료 |
| `search_completed_at` | timestamp(ms) | O | FAISS 검색 완료 |
| `card_render_done_at` | timestamp(ms) | O | 화면 카드 렌더링 완료 |
| `status` | enum | O | `success` / `fail` |
| `error_code` | string | △ | 실패 시 오류 코드 |
| `is_warmup` | bool | △ | 콜드 스타트 제외 대상 여부 (**추가 권고**) |
| `concurrency` | int | △ | 동시 요청 수 (**추가 권고**) |

## 7.5 `schema_validation_log.csv`

| 컬럼명 | 타입 | 필수 | 설명 |
|---|---|:---:|---|
| `run_id` | string | O | 실행 ID |
| `artwork_id` | string | O | 작품 ID |
| `schema_version` | string | O | 검증에 사용한 스키마 버전 (**추가 권고**) |
| `first_pass_valid` | bool | O | 1차 JSON 스키마 통과 여부 |
| `retry_count` | int | O | 재시도 횟수 |
| `final_valid` | bool | O | 최종 스키마 통과 여부 |
| `validation_error` | string | △ | 실패 사유 원문 |
| `error_class` | enum | △ | 필드누락/타입불일치/허용값위반/파싱실패 (**추가 권고**) |

## 7.6 `top5_relevance_eval.csv`

| 컬럼명 | 타입 | 필수 | 설명 |
|---|---|:---:|---|
| `query_artwork_id` | string | O | 검색 기준 작품 ID |
| `search_axis` | enum | O | 도상 / 기법 / 물성 / 색채 / 가중 |
| `rank` | int | O | 검색 결과 순위 (1~5) |
| `result_artwork_id` | string | O | 추천 유사작 ID |
| `curator_score_0_2` | int | O | 큐레이터 관련도 점수 (0/1/2) |
| `curator_id` | string | △ | 평가자 ID (이중 평가 식별, **추가 권고**) |
| `curator_comment` | string | △ | 평가 메모 |

## 7.7 `practitioner_survey.csv`

| 컬럼명 | 타입 | 필수 | 설명 |
|---|---|:---:|---|
| `reviewer_id` | string | O | 실무자 ID (익명 코드) |
| `org_type` | enum | △ | 공립미술관 / 사립미술관 / 갤러리 (**추가 권고**) |
| `career_band` | enum | △ | 경력 구간 (**추가 권고**) |
| `blind_condition` | string | O | 블라인드 조건 코드 |
| `likert_score_1_5` | int | O | 실무 사용 의향 점수 |
| `trust_score_1_5` | int | △ | 산출물 신뢰도 (**추가 권고**) |
| `expected_edit_min` | int | △ | 예상 수정 소요 시간(분) (**추가 권고**) |
| `least_trusted_layer` | enum | △ | 가장 신뢰 못 할 층위 (**추가 권고**) |
| `comment` | string | △ | 정성 코멘트 |

## 7.8 추가 파일 3종

**`annotator_agreement.csv`** — `layer`, `n_double_labeled`, `krippendorff_alpha`, `cohen_kappa`, `n_adjudicated`

**`fault_injection_log.csv`** — `injection_type`, `trial_id`, `fallback_entered`, `output_nonnull`, `pipeline_completed`, `error_code`

**`run_config.yaml`** — 3.7절 재현성 고정 항목 전체 + 각 파일 SHA-256

## 7.9 저장소 구조 및 무결성

```
kart-lens-eval/
├── protocol/
│   ├── protocol_v2.0.md          # 본 문서 (해시 고정)
│   ├── labeling_guide.md          # 라벨링 지침
│   ├── synonym_map.csv            # 동의어 사전 (동결)
│   └── kart_lens_4layer.schema.json
├── data/
│   ├── gold_labels.csv            # 동결 후 수정 금지
│   ├── predictions.jsonl
│   ├── latency_log.csv
│   ├── schema_validation_log.csv
│   ├── top5_relevance_eval.csv
│   ├── practitioner_survey.csv
│   ├── annotator_agreement.csv
│   └── fault_injection_log.csv
├── config/
│   └── run_config.yaml
├── scripts/
│   └── compute_metrics.py         # 데이터 확보 이전에 작성 완료
├── results/
│   ├── results_v3.0.md            # 자동 생성
│   └── error_taxonomy.csv
└── MANIFEST.sha256                # 전 파일 해시
```

## 7.10 데이터 거버넌스

| 항목 | 원칙 |
|---|---|
| 개인정보 | 실무자·라벨러는 익명 코드로만 기록. 실명·소속 상세 비수집 |
| 이미지 저작권 | 해외 CC0 데이터(Met·AIC·Rijksmuseum)와 국내 OpenAPI 데이터의 이용 조건은 **동일하지 않다.** e뮤지엄·국가문화유산포털 데이터의 재배포·상업적 이용 조건을 별도 확인하고 그 결과를 `LICENSE_NOTES.md`에 기록한다 |
| 이미지 저장 | 골드셋은 원본 대신 해시와 경로만 저장소에 포함, 원본은 별도 관리 |
| 로그 보존 | 실행 로그는 발표 후 최소 12개월 보존 |
| EXIF | 전처리 단계에서 위치정보 등 EXIF 전량 제거 |

---

# 8. 기존 문구 수정 대조표 (전면판)

## 8.1 평가 방법 관련 (보완안 반영)

**[1] 골드셋 일치율**

- **기존:** "큐레이터가 직접 4-Layer 라벨을 작성한 골드셋 100점을 기준으로 필드 단위 일치율을 측정했습니다."
- **수정:** "큐레이터가 직접 작성한 4-Layer 골드셋을 기준으로 필드 단위 일치율을 측정하도록 설계했습니다. 실측 결과는 골드셋과 모델 출력 로그를 매칭해 산출하며, 색채 90%, 물성 80%, 기법 65%, 도상 55%를 목표 기준으로 설정했습니다. 표본 수는 도상 지표의 신뢰구간 반폭을 ±8%p 이내로 확보하기 위해 N=150을 권고합니다."

**[2] 시스템 안정성**

- **기존:** "P95 처리 시간이 15초 이내인지, JSON 스키마 1차 통과율이 95% 이상인지를 시스템 안정성 지표로 사용했습니다."
- **수정:** "시스템 안정성은 업로드 시작부터 카드 렌더링 완료까지의 P95 처리시간과 JSON 스키마 1차 통과율로 측정합니다. 목표 기준은 P95 15초 이내, JSON 1차 통과율 95% 이상이며, P95는 성공 실행 100건 이상에서 콜드 스타트를 제외하고 산출합니다."

**[3] 실무 수용성**

- **기존:** "실무 수용성(목표 3.5 이상)을 검증했습니다."
- **수정:** "실무 수용성은 실무자 5명의 5점 리커트 평균으로 측정합니다. 목표 기준은 3.5점 이상이나, n=5는 통계적 판정이 불가능한 규모이므로 평균·중앙값·개별 점수·코멘트를 원자료 그대로 제시하고 참고 신호로만 활용합니다."

**[4] 검색 품질**

- **기존:** "관련성 60% 이상을 달성했는지 측정했습니다."
- **수정:** "검색된 유사작 Top-5를 큐레이터가 3점 척도로 채점하고, 보수적 기준(2점만 관련 인정)으로 관련성 비율을 산출하도록 설계했습니다. 목표 기준은 60% 이상이며, 판정 기준을 표에 명시합니다."

## 8.2 추가 발견 항목 (본 보고서에서 신규 지적)

**[5] 물성 개선폭 — 최우선 수정**

- **기존:** "물성 정확도를 10%p 이상 끌어올리는 개선을 시도했습니다."
- **수정:** "프롬프트에 한국화·전통 재료군 20종을 제한 리스트로 주입하여 자유 생성을 분류 문제로 축퇴시키는 개선을 적용했습니다. **개선 효과의 크기는 제약 어휘 적용 전/후 대조 실험으로 측정할 예정이며, 현 시점에서 수치는 제시하지 않습니다.**"

**[6] Fail-safe**

- **기존:** "출력이 0이 되는 상황을 구조적으로 차단했습니다."
- **수정:** "치명도가 높은 색채·물성 층위에 규칙 기반 폴백 경로를 두어 출력 공백을 차단하도록 설계했습니다. 실효성은 장애 주입 시험으로 확인할 예정입니다."

**[7] 베이스라인**

- **기존:** "작품 한 점당 20분에서 40분의 시간을 들여"
- **수정:** "현장 인터뷰 기준 작품 한 점당 20~40분이 소요되는 것으로 파악되며, 정식 타임스터디는 후속 과제로 설계했습니다."

**[8] CSV 정합성**

- **기존:** "미술관 소장품 DB 컬럼 헤더와 정확히 일치하는 형식으로 데이터가 다운로드되어"
- **수정:** "대상 기관 소장품 DB의 컬럼 정의서를 기준으로 매핑표를 작성하고, 그 매핑에 따라 CSV를 내보내도록 설계했습니다. 현재 매핑 대상 기관은 `__`이며 매핑표는 부록에 첨부합니다."

**[9] 처리시간 약속**

- **기존:** "이 과정을 15초 이내로 단축하고"
- **수정:** "이 과정의 서비스 수준 목표(SLO)를 P95 15초로 설정했습니다."

---

# 9. 차별점 및 비교 평가 설계

## 9.1 4축 분리의 구조적 이점

구글 렌즈 등 기존의 단일 임베딩 검색은 결과가 틀렸을 때 **무엇이 틀렸는지 추적할 수 없다.** 유사도 점수 하나만 남기 때문에 색채가 비슷해서 뽑힌 것인지, 도상이 비슷해서인지 분해할 수 없고, 따라서 개선 방향도 도출되지 않는다.

K-ART LENS는 검색 축을 4개로 분리함으로써 다음을 얻는다.

| 이점 | 설명 |
|---|---|
| 복합 질의 대응 | '도상 단일 검색', '물성 가중 검색' 등 실무의 실제 질의 형태에 직접 대응 |
| 오류 귀인 가능 | 추천 실패 시 어느 축의 임베딩이 문제인지 축별 관련성 점수로 분해 |
| 점진적 개선 | 축별로 독립 개선 가능. 한 축의 교체가 다른 축을 훼손하지 않음 |
| 설명 가능성 | 큐레이터에게 "이 작품이 추천된 이유는 물성 유사도"라고 제시 가능 |
| 하류 재사용 | 4축 태그가 그대로 후속 K-VALUE 모델의 입력 피처가 됨 |

## 9.2 공정 비교 조건 (현재 미실시)

"기존 대비 우수하다"는 주장은 **비교 조건을 명시하지 않으면 성립하지 않는다.** 본 MVP는 아직 비교 실험을 수행하지 않았으며, 다음을 설계로만 제시한다.

| 비교 대상 | 비교 조건 | 측정 지표 |
|---|---|---|
| 범용 VLM 단독 (제약 없음) | 동일 이미지·동일 골드셋 | 4-Layer 일치율, JSON 1차 통과율 |
| 단일 임베딩 검색 (CLIP only) | 동일 코퍼스·동일 쿼리 | Top-5 관련성, nDCG@5 |
| 수기 입력 | 동일 작품 5점, 실무자 3인 | 소요시간, 입력 편차 |

> **발표 시 유의:** 슬라이드에 구글 렌즈를 비교 대상으로 표기할 경우, 실측 비교를 하지 않았다면 **"구조적 차이"**라고만 서술하고 성능 우위를 주장하지 않는다.
---

# 10. 한계 및 타당도 위협 (Threats to Validity)

성능이 좋게 나왔을 때 그 결과를 무효화할 수 있는 요인들을 **결과가 나오기 전에** 열거한다.

## 10.1 구성 타당도 (Construct Validity)

| 위협 | 설명 | 완화 조치 |
|---|---|---|
| 골드셋이 정답이라는 가정 | 큐레이터 1인의 판단이 유일한 정답은 아님 | 이중 라벨링, α 보고, 인간 상한 병기 |
| 일치율 ≠ 유용성 | 형식적으로 일치해도 실무에 무용할 수 있음 | 실무 수용성 설문 병행, 수정 소요시간 문항 추가 |
| 관련성 ≠ 발견 가치 | 이미 아는 작품만 추천되면 관련성은 높아도 무용 | 코멘트 정성 코딩에 '새로운 발견' 항목 포함 |
| 리커트 의향 ≠ 실사용 | 의향과 행동의 괴리 | 후속 단계에서 실제 사용 로그 측정 |

## 10.2 내적 타당도 (Internal Validity)

| 위협 | 설명 | 완화 조치 |
|---|---|---|
| 데이터 누수 | 골드셋이 인덱스·프롬프트 예시에 포함 | 4.4.4절 차단 조치, 코퍼스 해시 대조 |
| 사후 기준 이동 | 결과를 본 뒤 동의어·가중치·기준 조정 | 사전등록, 파일 동결, 채점 스크립트 선작성 |
| 반복 실행 선택 | 여러 번 돌려 좋은 결과만 채택 | 실행 횟수 로그 기록, 최종 실행 1회 지정 |
| 평가자 앵커링 | 라벨러가 모델 출력을 먼저 봄 | 블라인드 라벨링, 결과 무작위 배열 |

## 10.3 외적 타당도 (External Validity)

| 위협 | 설명 | 완화 조치 |
|---|---|---|
| 코퍼스 편향 | 오픈 데이터 중심이라 실제 소장품 분포와 불일치 | 층화 추출로 재료 계열 최소 배분 강제 |
| 촬영 조건 편향 | 미술관 공식 촬영본 위주, 현장 스냅 부재 | 이미지 품질 서브그룹 분석 |
| 기관 일반화 | 특정 기관 스키마에 맞춘 결과일 수 있음 | 매핑표 다기관 확장, 기관 무관 필드 우선 |
| 실무자 5인의 대표성 | 표본이 작고 선택 편향 가능 | 소속 유형·경력 구간 기록, 판정 미실시 |

## 10.4 통계적 결론 타당도 (Statistical Conclusion Validity)

| 위협 | 설명 | 완화 조치 |
|---|---|---|
| 검정력 부족 | N=150에서 도상 반폭 ±8%p | 판정 규칙에 '보류' 범주 명시적 포함 |
| 다중 비교 | 8개 지표 동시 판정 시 FWER 33.7% | 1차 종점 3개 지정 + Bonferroni |
| P95의 불안정성 | 소표본에서 극단값에 민감 | N≥100, 부트스트랩 CI, P50/P90/P99 병기 |
| 서브그룹 과소표집 | 계열별 N이 30 내외 | 서브그룹은 참고치로만, 판정 안 함 |

## 10.5 인정하는 한계 (발표에서 먼저 말할 것)

1. 본 MVP는 **초안 생성기**이며 확정 메타데이터 생성기가 아니다. 큐레이터 검수를 반드시 전제한다.
2. 작가 동정·진위·가격 판단은 범위 밖이며, 산출물이 그 용도로 오용되지 않도록 UI에 명시할 필요가 있다.
3. 현 단계에서 성능에 대한 어떠한 정량 주장도 하지 않는다.
4. 2주 개발 기간의 제약상 코퍼스 규모·다양성이 실제 기관 소장품 분포를 대표하지 못한다.

> 한계를 먼저 말하는 것은 방어가 아니라 신뢰의 확보다. 심사자가 먼저 지적하는 한계와 발표자가 먼저 밝히는 한계는 청중에게 전혀 다르게 들린다.

---

# 11. 회고(KPT) 및 향후 계획

## 11.1 KPT — 주장 강도 조정판

**Keep (유지할 점)**

신규 모델을 처음부터 학습시키는 대신, 사전학습 모델의 조합과 **도메인 스키마 설계**에 자원을 집중하여 2주라는 기간 안에 파이프라인을 멈춤 없이 완주시킨 전략을 유지한다. 특히 "모델을 바꾸는 것보다 출력 형식을 강제하는 것이 실무 적용에 더 큰 영향을 준다"는 판단은 유효했다.

**Problem (문제점)**

초기에 VLM이 장지에 분채로 그려진 한국화를 아크릴로 오인식하는 등, **물성 층위에서 서양 미술 편향**이 확인되었다. 더 근본적인 문제는, 이 편향을 발견했음에도 **개선 효과를 측정할 대조군 설계를 사전에 마련하지 않았다는 점**이다. 그 결과 개선을 적용하고도 그 효과를 정량화할 수 없는 상태가 되었다. 이것이 본 프로젝트의 가장 큰 방법론적 실책이다.

**Try (시도할 점)**

- 프롬프트에 한국화·전통 재료군 20종을 제한 리스트로 강제 주입하여 자유 생성을 분류 문제로 축퇴시켰다. **효과 크기는 4.12절 절제 실험으로 측정한다.**
- 개선을 적용하기 **전에** 대조군 실행 로그를 먼저 남기는 것을 표준 절차로 삼는다.
- 해외 오픈 액세스(CC0) 데이터를 보강하되, 그것이 오히려 서양화 편향을 강화할 수 있음을 인지하고 **국내 재료 계열 데이터 확보를 우선**한다.

## 11.2 Future Work

| 단계 | 내용 | 선행 조건 |
|---|---|---|
| 즉시 | 골드셋 구축 및 9종 로그 확보, v3.0 결과 확정 | 12절 스프린트 실행 |
| 단기 | 도상 어휘집(십장생·문자도·산수 등) 구축, 도상 층위 성능 개선 | 오류 분석 E-ICO-01 결과 |
| 단기 | 대상 기관 DB 컬럼 매핑표 작성, 실제 적재 파일럿 | 협력 기관 1곳 확보 |
| 중기 | 4-Layer 태그를 입력 피처로 하는 **K-VALUE** 가치평가 모델 | 4-Layer 태그 신뢰도 확보 |
| 중기 | 유찰 데이터의 관측 편향 통계 보정(선택 편향 보정) | K-VALUE 설계 |

> **K-VALUE 연계 시 반드시 유의할 점:** 본 MVP의 4-Layer 태그가 후속 가치평가 모델의 입력 피처가 된다는 것은, **4-Layer의 측정 오차가 가치평가 모델의 편향으로 전파된다**는 뜻이다. 층위별 일치율과 신뢰구간은 K-VALUE 단계에서 피처 노이즈 수준의 사전 정보로 사용되어야 하며, 태그를 오차 없는 관측치로 취급해서는 안 된다. 이것이 본 보고서가 신뢰구간을 고집하는 실질적 이유다.

---

# 12. 실행 계획 — 데이터 확보 스프린트 (D+0 ~ D+14)

| 일차 | 작업 | 산출물 | 예상 공수 |
|---|---|---|---|
| D+0 | 프로토콜 v2.0 확정, 해시 기록 | `protocol_v2.0.md` | 2h |
| D+0~1 | 라벨링 지침·동의어 사전 작성 및 동결 | `labeling_guide.md`, `synonym_map.csv` | 6h |
| D+1 | 채점 스크립트 작성 및 더미 데이터 검증 | `compute_metrics.py` | 6h |
| D+2~3 | 파일럿 골드셋 20점 이중 라벨링 | 파일럿 라벨 | 8h |
| D+3 | 파일럿 α 산출, 지침 개정 여부 판단 | `annotator_agreement.csv` (파일럿) | 2h |
| D+4~7 | 본 골드셋 150점 라벨링 (30% 이중) | `gold_labels.csv` (동결) | 30h |
| D+8 | `run_config.yaml` 고정, 본 실행 1회 | `predictions.jsonl` 외 3종 | 4h |
| D+8 | 지연시간 측정 200회 실행 | `latency_log.csv` | 3h |
| D+9 | 장애 주입 시험 (5유형 × 20회) | `fault_injection_log.csv` | 4h |
| D+9 | 절제 실험 A/B 실행 | 대조군 로그 | 3h |
| D+10~11 | Top-5 관련도 채점 300건 | `top5_relevance_eval.csv` | 10h |
| D+12 | 실무자 5인 블라인드 설문 | `practitioner_survey.csv` | 5h |
| D+13 | 채점 스크립트 실행, 결과 자동 산출 | `results_v3.0.md` | 2h |
| D+13 | 오류 분류 및 서브그룹 분석 | `error_taxonomy.csv` | 4h |
| D+14 | v3.0 보고서·발표자료 갱신 | 최종본 | 6h |

**총 예상 공수 약 95시간.** 그중 골드셋 라벨링이 30시간으로 최대 비중이다.

## 12.1 공수 부족 시 축소 우선순위

| 우선순위 | 항목 | 축소 시 영향 |
|:---:|---|---|
| 1 (사수) | 골드셋 N≥100 + 이중 라벨링 30% | 이것이 없으면 어떤 정확도 주장도 불가 |
| 2 (사수) | `latency_log.csv` N≥100 | P95는 로그만 있으면 저비용으로 확보 가능 |
| 3 (사수) | `schema_validation_log.csv` | 자동 생성 가능, 비용 거의 없음 |
| 4 | 절제 실험 | 축소 시 물성 개선 주장을 **전면 삭제** |
| 5 | Top-5 관련도 300건 → 150건 | 축소 시 CI 폭 확대, 판정 '보류' 가능성 증가 |
| 6 | 실무자 설문 | 축소 시 해당 행을 표에서 제거 (판정 없음이므로 손실 적음) |

> 축소 판단의 원칙: **비용 대비 주장 가치가 가장 낮은 것부터 버린다.** 지연·스키마 로그는 사실상 공짜이므로 절대 빠뜨리지 않는다. 반대로 실무자 설문은 공수 대비 판정 기여가 없으므로 가장 먼저 축소 대상이다.

---

# 13. 최종 결론 문구 템플릿

아래 문구는 **9종 원시 데이터가 확보되고 `compute_metrics.py`로 수치가 산출된 이후** 최종 발표 결론에서 사용한다.

> "측정 결과, K-ART LENS는 4-Layer 중 `__개 / 4개` 지표에서 목표 기준을 충족했습니다. 특히 `가장 높은 지표명`은 `__%` (95% CI `__~__`)로 목표 `__%`를 상회했으며, `가장 낮은 지표명`은 `__%` (95% CI `__~__`)로 추가 개선이 필요했습니다. 참고로 동일 층위의 인간 라벨러 간 일치율은 `__%`였습니다.
>
> 시스템 안정성 측면에서는 P95 처리시간 `__초` (부트스트랩 CI `__~__`), JSON 스키마 1차 통과율 `__%`를 기록해 각각 `달성/미달/보류`로 판정했습니다. 유사작 Top-5 관련성은 보수적 기준으로 `__%`였습니다. 실무 수용성은 평균 `__ / 5.0` (n=5)이며, 표본이 작아 판정은 하지 않고 참고 신호로만 제시합니다.
>
> 사전에 지정한 1차 종점 3개 중 `__개`가 달성으로 판정되었으므로, 본 MVP는 `제한적 실무 적용 가능 / 추가 검증 필요 / 목표 충족`으로 판단됩니다. 다만 `__` 계열에서 최악 그룹 성능이 `__%`로 나타나 해당 계열 기관에는 아직 권장하지 않습니다."

**결론 서술 시 금지 사항**

- 1차 종점 판정 없이 2차 종점만으로 "목표 충족" 선언
- 신뢰구간 없이 점추정만 제시
- '보류' 판정을 '달성'으로 반올림
- 최악 그룹 성능을 생략한 채 전체 평균만 제시

---

# 부록

## 부록 A. 4-Layer JSON 스키마 초안 (v0.1)

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "K-ART LENS 4-Layer Artwork Record",
  "type": "object",
  "required": ["artwork_id", "iconography", "technique", "materiality", "color", "meta"],
  "properties": {
    "artwork_id": { "type": "string" },
    "iconography": {
      "type": "object",
      "required": ["tags", "abstained"],
      "properties": {
        "tags":       { "type": "array", "items": { "type": "string" }, "maxItems": 5 },
        "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
        "abstained":  { "type": "boolean" }
      }
    },
    "technique": {
      "type": "object",
      "required": ["primary", "abstained"],
      "properties": {
        "primary":    { "type": ["string", "null"] },
        "secondary":  { "type": "array", "items": { "type": "string" }, "maxItems": 3 },
        "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
        "abstained":  { "type": "boolean" }
      }
    },
    "materiality": {
      "type": "object",
      "required": ["primary", "abstained"],
      "properties": {
        "primary": {
          "type": ["string", "null"],
          "description": "부록 B 제약 어휘에서만 선택",
          "enum": ["장지에 분채", "장지에 수간채색", "순지에 수묵", "화선지에 수묵담채",
                   "견본채색", "마본채색", "저본채색", "지본수묵", "지본담채", "지본채색",
                   "캔버스에 유채", "캔버스에 아크릴", "패널에 혼합재료", "종이에 콜라주",
                   "목조", "석조", "금속", "도자", "옻칠·나전", "사진·판화", null]
        },
        "support":    { "type": ["string", "null"], "description": "바탕재" },
        "medium":     { "type": ["string", "null"], "description": "채색재" },
        "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
        "abstained":  { "type": "boolean" }
      }
    },
    "color": {
      "type": "object",
      "required": ["palette", "groups", "abstained"],
      "properties": {
        "palette":    { "type": "array", "items": { "type": "string", "pattern": "^#[0-9A-Fa-f]{6}$" }, "maxItems": 5 },
        "groups":     { "type": "array", "items": { "type": "string" }, "maxItems": 3 },
        "saturation_band": { "type": "string", "enum": ["저채도", "중채도", "고채도"] },
        "abstained":  { "type": "boolean" }
      }
    },
    "caption_ko": { "type": "string", "maxLength": 500 },
    "meta": {
      "type": "object",
      "required": ["run_id", "model_version", "prompt_version", "schema_version", "fallback_used"],
      "properties": {
        "run_id":         { "type": "string" },
        "model_version":  { "type": "string" },
        "prompt_version": { "type": "string" },
        "schema_version": { "type": "string" },
        "fallback_used":  { "type": "boolean" }
      }
    }
  },
  "additionalProperties": false
}
```

> **주의:** 위 `enum` 목록은 초안이다. 실제 운용 중인 제약 어휘 20종 원본 파일로 교체해야 하며, 교체 시 `schema_version`을 올리고 그 이전 통과율 로그와 분리 보관한다.

## 부록 B. 물성 제약 어휘 — 초안 및 확정 절차

**초안 20종 (지본·견본 계열 우선)**

| 구분 | 어휘 |
|---|---|
| 지본 | 장지에 분채, 장지에 수간채색, 순지에 수묵, 화선지에 수묵담채, 지본수묵, 지본담채, 지본채색 |
| 직물본 | 견본채색, 마본채색, 저본채색 |
| 서양화 | 캔버스에 유채, 캔버스에 아크릴, 패널에 혼합재료, 종이에 콜라주 |
| 입체·기타 | 목조, 석조, 금속, 도자, 옻칠·나전, 사진·판화 |

**확정 절차:** (1) 대상 기관 소장품 DB의 실제 재료 필드 값 분포를 빈도순으로 추출 → (2) 상위 누적 90%를 커버하는 어휘로 목록 재구성 → (3) 큐레이터 2인 검토 → (4) 파일 해시 기록 후 동결. 어휘가 실제 분포를 커버하지 못하면 물성 일치율은 어휘 설계 실패로 인해 구조적으로 낮게 나온다.

## 부록 C. 라벨링 지침 요약

| 층위 | 라벨 규칙 | 경계 사례 처리 |
|---|---|---|
| 도상 | 화면에서 식별 가능한 주제 태그 최대 3개, 중요도 순 | 추상 작품은 `추상` 단일 태그, 억지 해석 금지 |
| 기법 | 주 기법 1개 필수, 보조 최대 2개 | 혼합 기법은 화면 점유 면적이 큰 쪽을 주 기법으로 |
| 물성 | 제약 어휘에서 주라벨 1개 선택 필수 | 육안 판별 불가 시 `판단 유보` 선택. 추측 금지 |
| 색채 | 대표 색군 최대 3개, 면적 순 | 액자·배경·매트는 제외. 화면 영역만 판단 |

**공통 규칙**

- 작품 정보(작가·제목·연도)를 보지 않고 **이미지만으로** 라벨링한다.
- 확신이 없으면 `label_confidence`를 1로 기록하고 넘어간다. 억지로 채우지 않는다.
- 라벨링 중 지침의 모호함을 발견하면 즉시 기록하고, 지침 개정은 **파일럿 단계에서만** 허용한다.

## 부록 D. 채점 스크립트 (`compute_metrics.py`) — **본 보고서와 함께 제출**

본 보고서는 산식을 문장으로만 남기지 않는다. **판정 규칙을 코드로 고정한 실행 가능한 스크립트를 함께 제출**하며, 자체 검증(`--selftest`) 21개 항목이 전부 통과한 상태다. 실측 데이터가 확보되면 이 스크립트를 실행하는 것만으로 본 보고서 5장의 결과표가 생성된다.

### D.1 실행 방법

```bash
python3 compute_metrics.py --selftest              # 통계 함수 자체 검증 (21항목)
python3 compute_metrics.py --data ./raw --out ./results
```

Python 3.8 이상, **외부 의존성 없음**(표준 라이브러리만 사용). 별도 환경 구축 없이 실무자 PC에서 바로 재현 가능하도록 의도한 설계다.

### D.2 기능 구성

| 기능 | 입력 | 출력 | 구현 방식 |
|---|---|---|---|
| 층위별 일치율 + Wilson CI | `gold_labels.csv`, `predictions.jsonl`, `synonym_map.csv` | 일치율, CI, 판정 | 층위별 채점 규칙(§4.5.2) 코드 고정 |
| 라벨 정규화 | 동일 | 정규화 라벨 | NFC → 괄호 주석 제거 → 접미 제거 → 동의어 사전 |
| 이중 라벨 중복 제거 | `gold_labels.csv` | 작품당 최종 골드 1행 | `adjudicated` 우선, 미지정 시 최초 행 |
| P95 + 부트스트랩 CI | `latency_log.csv` | P50/P90/P95/P99/최댓값, CI, 실패율 | 선형보간 백분위 + 2,000회 재표본 |
| JSON 통과율 | `schema_validation_log.csv` | 1차·최종 통과율, 재시도율, 실패 유형 분포 | Bonferroni z=2.394 적용 |
| Top-5 관련성 | `top5_relevance_eval.csv` | 엄격·완화 관련성, 축별 분해 | **질의 단위 클러스터 부트스트랩** |
| 라벨러 간 신뢰도 | `gold_labels.csv` (이중 라벨분) | 쌍별 일치율, Krippendorff α | 명목 척도 α 구현 |
| 리커트 기술통계 | `practitioner_survey.csv` | 평균·중앙값·최소·최대·표준편차·개별 점수 | **판정 수행하지 않음** |
| 개선 효과 검정 | 절제 실험 로그 | McNemar 정확검정 p값 | 대응표본 이항 정확검정 |
| 결과표 자동 생성 | 전체 | `results_summary.csv`, `results_table.md`, `results_detail.json` | 본 보고서 5장 표 형식과 동일 |

### D.3 데이터가 없을 때의 동작 — 가장 중요한 사양

원시 데이터가 없으면 스크립트는 **오류를 내거나 0을 출력하지 않고, 해당 행을 `미측정`으로 유지한 채 공란 표를 그대로 출력한다.**

```
| 시스템 안정성 | JSON 스키마 1차 통과율 | 1차 | 95% 이상 | `__ / __ = __%` | `[__, __]` | 미측정 | 원시 데이터 없음 |

[경고] 미측정 지표가 있습니다: 색채 일치율, 물성 일치율, ...
       해당 항목은 발표 자료에서 공란으로 유지하십시오.
```

즉 **현 시점에 이 스크립트를 실행하면 본 보고서 5장의 표가 그대로 재현된다.** 5장의 공란은 작성자의 서술 선택이 아니라 **도구가 산출한 결과**이며, 이것이 "근거 없는 수치는 기입하지 않는다"는 원칙의 기계적 보증이다.

### D.4 자체 검증에 포함된 항목

Wilson 구간(92/100 → [85.0%, 95.9%]), 판정 임계 카운트(§4.6.1 전 항목), 필요 표본 수(§4.4.2 전 항목), 선형보간 백분위, McNemar 정확검정, Krippendorff α, 층위별 채점 규칙 5종, 3분 판정 규칙 5종. **본 보고서에 인쇄된 모든 수치는 이 검증을 통과한 함수로 산출되었다.**

### D.5 설계 원칙

스크립트는 **판정까지 자동 수행**한다. 사람이 개입해 판정을 바꿀 수 있는 지점을 남기지 않는 것이 사후 기준 이동을 막는 마지막 방어선이다. 판정 파라미터(`Z_PRIMARY`, `Z_SECONDARY`, `TARGETS`, `MATCH_RULE`)는 파일 상단에 상수로 모아 두었으며, **이 값의 변경은 곧 프로토콜 개정**이므로 변경 시 해시를 재기록하고 결과표에 변경 이력을 명시한다.

## 부록 E. 예상 질의응답 10선

**Q1. 왜 성능 수치가 하나도 없습니까?**
> 근거 로그 없이 산출된 수치를 발표 자료에 올리지 않기로 원칙을 세웠기 때문입니다. 대신 측정식·표본 설계·판정 규칙을 모두 확정해 두었고, 로그가 확보되면 해석의 여지 없이 결론이 나옵니다. 소요 공수는 약 95시간으로 산정했습니다.

**Q2. 골드셋 100점이면 충분하지 않나요?**
> 색채(목표 90%)에는 충분하지만 도상(목표 55%)에는 부족합니다. N=100에서 도상 일치율의 신뢰구간 반폭이 약 ±10%p여서, 55%가 나와도 목표 달성 여부를 판정할 수 없습니다. N=150을 권고합니다.

**Q3. 목표 기준을 층위별로 다르게 잡은 근거는?**
> 층위별 난이도가 다르고, 평균을 내면 색채의 높은 점수가 도상의 실패를 은폐하기 때문입니다. 평균 지표를 아예 만들지 않은 것도 같은 이유입니다.

**Q4. 물성 정확도가 10%p 올랐다는 건 어떻게 쟀습니까?**
> 정확히 그 지적 때문에 해당 수치를 이번 판본에서 삭제했습니다. 인과 주장에는 대조군이 필요한데 없었습니다. 제약 어휘 적용 전/후 A/B 절제 실험을 설계해 두었고, McNemar 검정으로 개선폭의 신뢰구간을 산출할 예정입니다.

**Q5. 실무자 5명이면 표본이 너무 적지 않나요?**
> 맞습니다. 그래서 이 지표는 판정 대상에서 제외했습니다. n=5의 95% 신뢰구간은 약 ±1.0점이라 3.5 기준을 가를 수 없습니다. 평균 대신 개별 점수 5개와 코멘트를 원자료로 제시합니다.

**Q6. 15초 목표는 어떻게 정했습니까?**
> 실무 흐름이 끊기지 않는 체감 상한을 기준으로 설정한 서비스 수준 목표입니다. 다만 P95만으로는 체감 지연을 다 설명하지 못해서 P50·P90·P99를 함께 보고하도록 했습니다.

**Q7. 구글 렌즈보다 나은 겁니까?**
> 실측 비교를 하지 않았으므로 성능 우위는 주장하지 않습니다. 구조적 차이는 명확합니다. 단일 임베딩은 무엇 때문에 유사하다고 판단했는지 분해할 수 없고, 그래서 개선 방향도 나오지 않습니다.

**Q8. JSON 통과율 95%면 실무에 바로 쓸 수 있는 건가요?**
> 아닙니다. 스키마 통과는 형식 유효성일 뿐 내용 정확성이 아닙니다. 통과율이 높은데 일치율이 낮으면 형식만 완벽하고 내용은 틀린 데이터를 대량 생산하는 상태이며, 이는 아무것도 만들지 않는 것보다 위험합니다. 두 지표는 반드시 함께 봅니다.

**Q9. 폴백이 있으면 항상 결과가 나온다는 뜻인가요?**
> 설계상 그렇게 의도했지만 아직 장애 주입 시험을 하지 않았습니다. 5개 장애 유형에 각 20회씩 주입해 폴백 진입률을 확인할 계획이며, 그 전까지는 "차단했다"가 아니라 "차단하도록 설계했다"로 말합니다.

**Q10. 이 결과가 K-VALUE로 어떻게 이어집니까?**
> 4-Layer 태그가 가치평가 모델의 입력 피처가 됩니다. 그래서 태그의 측정 오차가 그대로 가치평가 편향으로 전파됩니다. 층위별 신뢰구간을 확보해 두어야 후속 단계에서 피처 노이즈를 사전 정보로 반영할 수 있습니다. 신뢰구간을 고집하는 실질적 이유가 여기 있습니다.

## 부록 F. 제출 전 체크리스트

**문서**

- [ ] 완료형 동사("측정했다/검증했다/달성했다")가 남아 있지 않은가
- [ ] 로그 근거 없는 수치(소수점 포함)가 남아 있지 않은가
- [ ] 모든 비율에 `분자/분모` 및 `N`이 병기되었는가
- [ ] Top-5 관련 판정 기준이 표 하단에 명시되었는가
- [ ] 스키마·프롬프트·모델 버전이 결과표에 병기되었는가
- [ ] 비교 우위 주장이 실측 없이 들어가 있지 않은가

**데이터**

- [ ] 9종 파일이 모두 존재하고 `MANIFEST.sha256`이 갱신되었는가
- [ ] `gold_labels.csv`가 동결되었고 실행 후 수정 이력이 없는가
- [ ] 골드셋 `artwork_id`가 검색 인덱스에서 제외되었는가
- [ ] 이중 라벨링 비율이 30% 이상이고 α가 산출되었는가
- [ ] `latency_log.csv`의 콜드 스타트 제외 건수가 명시되었는가
- [ ] 실패 실행이 P95에서 제외되었고 실패율이 별도 보고되었는가

**분석**

- [ ] 모든 수치가 `compute_metrics.py` 출력에서 복사되었는가 (수기 계산 0건)
- [ ] 1차 종점 3개에 Bonferroni 보정이 적용되었는가
- [ ] 판정이 Wilson CI 규칙에 따라 자동 산출되었는가
- [ ] 서브그룹 표와 최악 그룹 성능이 포함되었는가
- [ ] 인간 라벨러 간 일치율이 모델 일치율과 나란히 제시되었는가

## 부록 G. 용어 정리

| 용어 | 정의 |
|---|---|
| 골드셋 (Gold set) | 정답으로 간주하는 인간 라벨 집합. 본 과제에서는 큐레이터 작성 4-Layer 라벨 |
| 인간 상한 (Human ceiling) | 동일 과제에서 인간 라벨러들끼리 일치하는 비율. 모델 성능 해석의 기준선 |
| Wilson 신뢰구간 | 이항 비율의 신뢰구간 추정법. 소표본·극단 비율에서 정규 근사보다 안정적 |
| P95 | 상위 5%를 제외한 최댓값. 꼬리 지연을 보는 지표 |
| 1차 종점 (Primary endpoint) | 성패 판정의 근거가 되는 사전 지정 지표 |
| FWER | 다중 비교 시 최소 1개 이상 거짓 양성이 나올 확률 |
| Bonferroni 보정 | 유의수준을 비교 횟수로 나누어 FWER을 통제하는 방법 |
| Krippendorff α | 다중 라벨·결측을 허용하는 평가자 간 신뢰도 계수 |
| 절제 실험 (Ablation) | 구성 요소를 제거·추가한 대조 실험으로 기여도를 분리하는 방법 |
| 판단 유보 (Abstention) | 신뢰도가 낮을 때 값을 생성하지 않고 유보를 반환하는 전략 |
| 사전등록 (Pre-registration) | 데이터 수집 전에 가설·지표·분석 방법을 고정 공개하는 절차 |
| 데이터 누수 (Leakage) | 평가 대상 정보가 학습·인덱스·프롬프트에 유입되어 성능이 부풀려지는 현상 |
| SLO | 서비스 수준 목표. 본 과제에서는 P95 15초 |

## 부록 H. 참고 자료

- e뮤지엄, 국가문화유산포털 OpenAPI
- The Metropolitan Museum of Art / Art Institute of Chicago / Rijksmuseum Open Access (CC0) Data
- 선행 판본: `K-ART_LENS_최종발표자료.pdf` (v1.0), `K-ART_LENS_최종발표보고서_보완본.pdf` (v1.5)

---

**문서 종료.** 본 보고서 v2.0은 평가 프로토콜 확정본이며, 실측값이 채워진 v3.0으로 대체될 때까지 성능에 관한 정량 주장의 근거로 인용될 수 없다.
